{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.horecaos.uz/events/tenancy.events/LocationCapacityCleared.v1.schema.json",
  "title": "LocationCapacityCleared v1 payload",
  "description": "A release just opened the first free concurrent-order slot. Identifiers only.",
  "type": "object",
  "additionalProperties": false,
  "required": ["locationId", "brandId", "maxConcurrentOrders"],
  "properties": {
    "locationId": { "type": "string", "format": "uuid" },
    "brandId": { "type": "string", "format": "uuid" },
    "maxConcurrentOrders": { "type": "integer", "minimum": 1 }
  }
}
