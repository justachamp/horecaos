/**
 * Keycloak identity links, application principals, authorization projections,
 * and tenant-scoped access enforcement.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Identity and Access")
package uz.qoida.platform.iam;
