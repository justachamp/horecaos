/**
 * The flow YAML schema: six block types, states, and the document they compose
 * into. Pure domain — no Spring, no Jackson, no channel SDK — parsed and
 * validated by {@link uz.horecaos.platform.conversations.domain.FlowDocumentParser}.
 */
package uz.horecaos.platform.conversations.domain;
