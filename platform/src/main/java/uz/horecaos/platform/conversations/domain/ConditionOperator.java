package uz.horecaos.platform.conversations.domain;

/** How a {@link ConditionBlock} compares a captured field against a value. */
public enum ConditionOperator {
    PRESENT,
    ABSENT,
    EQUALS
}
