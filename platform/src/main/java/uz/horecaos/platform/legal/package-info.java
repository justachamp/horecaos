/**
 * A tenant's own terms of service: versioned, authored per locale in the
 * tenant's own operations console, and served to the storefront with a
 * brand-neutral platform default for a tenant who has not written one
 * (ADR 0068).
 */
@org.springframework.modulith.ApplicationModule(displayName = "Legal documents")
package uz.horecaos.platform.legal;
