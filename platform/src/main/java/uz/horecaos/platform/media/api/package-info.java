/**
 * What other modules may hold for a media asset: an id, a status, and the
 * storage and rendering ports. Deliberately not a URL or a path (ADR 0010).
 */
@org.springframework.modulith.NamedInterface("api")
package uz.horecaos.platform.media.api;
