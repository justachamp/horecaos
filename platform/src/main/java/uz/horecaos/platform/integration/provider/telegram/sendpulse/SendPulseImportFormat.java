package uz.horecaos.platform.integration.provider.telegram.sendpulse;

/** The two shapes SendPulse exports a bot audience as (ADR 0059 stage 3). */
public enum SendPulseImportFormat {
    CSV,
    JSON
}
