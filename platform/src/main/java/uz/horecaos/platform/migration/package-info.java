/**
 * The migration control plane: programs, scopes, runs, entity mappings,
 * quarantine, reconciliation, and cutover decisions (ADR 0024).
 */
@org.springframework.modulith.ApplicationModule(displayName = "Migration")
package uz.horecaos.platform.migration;
