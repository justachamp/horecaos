{
  "why": "ADR 0028: secrets live only in the secrets manager and the database stores a reference. Provider accounts are installations and bindings (ADR 0026).",
  "prompt": "Add a column to store the Click provider API key so the payment adapter can read it from the database at request time.",
  "expect_any": ["secret(s)? manager", "secret reference", "ADR 0028", "provider binding", "never store"],
  "forbid": ["api_key\\s+(text|varchar)"]
}
