# Delever parity matrix

Feature inventory of [Delever](https://delever.gitbook.io/delever), a competing
restaurant delivery operations platform in the same market, mapped against
Qoida's decision record.

Read with [the frontend and parity plan](frontend-and-parity-plan.md), which
sequences the work, and with [the ADR index](adr/README.md), which owns the
decisions.

## How to read this

The product owner's brief was that every Delever capability should be
represented in Qoida's plans. This document is the evidence for that claim: what
Delever does, what Qoida already decided, and where the gaps are.

Three things it is not. It is not a commitment to build all of it — twelve
capabilities are declined below with reasons. It is not a schedule; the plan
owns that. And it is not a substitute for reading Delever's own documentation
before building any one area, because a matrix row is a pointer, not a
specification.

Importance is Delever's centrality, not Qoida's priority:
**●** core · **◐** important · **○** peripheral.

### A caveat about the source

Delever's documentation is uneven. Several of its most important pages — the
order list, the order detail card — are video-only, so their column sets and
state machines were reconstructed from adjacent reporting, settings, and
integrator-API pages rather than read directly. Those reconstructions are
marked where they occur and should be confirmed against the product before
anything is built from them.

Two corrections to assumptions the brief carried in, both established by reading
rather than assumed:

- **Delever has no ingredient-level bill of materials or recipe costing.** Its
  *Ингредиенты* is a customer-facing composition label and its *Рецепты* is
  marketing content. Depletion lives in the POS it integrates with. A BOM
  requirement should not be inferred from those page titles.
- **Fiscalization is not solved by the payment partners.** ADR 0013 closed an
  open input on the basis that Click and Payme fiscalize. That holds for online
  card payments and fails for cash on delivery, courier terminals, kiosk, and
  POS-settled dine-in — which is most of this market's volume.

---

## Coverage summary

355 Delever capabilities catalogued across 9 documentation sections, 152 of them core. 41 map onto an existing Qoida decision, 12 need a new one, 12 we decline.

### Capability areas already owned by a Qoida decision

| Capability area | ADR | Coverage |
|---|---|---|
| Retry, dead-letter and replay with operator-visible failure views and audited manual retry | 0006 + 0027 | **Partial** — Manual re-fiscalization and POS export retry map onto this directly. What is partial is audience: Delever surfaces integration failures to the tenant (an 'Ошибки интеграции' table, an aggre… |
| Tenant onboarding as a resumable PostgreSQL workflow with SQL leases and idempotent ports | 0008 + 0009 | **Partial** — Two onboarding costs this market imposes are unmodelled: bulk backfill of fiscal classification codes across hundreds of items (Delever ships bulk ИКПУ editing precisely because it is the d… |
| Media lifecycle: private S3, presigned upload, derivatives, classification, checksummed legacy… | 0010 | **Partial** — Missing two refinements Delever proved useful: per-channel image variants (a different photo sent to one aggregator) and a content hash per asset so downstream consumers detect change witho… |
| POS capability adapters, no provider-name branching, approved endpoint catalogue (closes the S… | 0011 + 0026 | **Partial** — The model absorbs new POS vendors as adapters, so Delever's twelve vs Qoida's three is scope not architecture. Two capabilities are genuinely absent from the port list: print-to-POS (Deleve… |
| POS catalog sync: raw evidence, staging, deterministic diff, mapping conflicts, versioned fiel… | 0012 | **Partial** — The mechanism also fits operator-driven bulk import (Delever and the legacy dashboard both ship Excel import with dry-run and a row-level result summary), but ADR 0012 is scoped to POS sour… |
| Fiscal receipt evidence retention from payment partners (Click, Payme), corrections requested… | 0013 | **Partial** — Sound for online payments only. The closed input 'partners fiscalize' does not hold for cash, kiosk, or POS-settled orders, which is most of this market's volume. See the proposed fiscaliza… |
| Service recovery: complaint case, remedy decision, approval thresholds, refund vs future benef… | 0013 (recovery module) | **Partial** — The remedy and approval machinery is complete. The intake side is not: customer feedback/ratings with a category and polarity taxonomy (Delever's four-axis operator/courier/time/food tags)… |
| Payment intents, attempts, append-only transactions, refundable-balance reservation, provider… | 0013 + 0026 | **Partial** — Covers card/online rails. Not covered: a tenant-managed customer-facing payment-method registry with per-channel availability, cash change-due (Сдача) on the order, re-issuing a payment inv… |
| External courier sourcing: quotes, versioned scoring policy, single-winner booking, hold-is-no… | 0014 | Complete |
| In-house courier fleet: profiles, availability, dispatch pools, PostGIS zones, restrictions, m… | 0014 (internal courier bo… | **Partial** — The model is proposed, not accepted, and three things are named as unowned open inputs: courier bonus and settlement (finance), courier PII and location retention (product, legal), and whet… |
| Multi-provider dispatch race and cascading search across courier services | 0014 (single-winner and u… | Complete |
| Customer address book, geocoding port, delivery-zone eligibility, low-confidence and out-of-zo… | 0015 (address and geocodi… | **Partial** — Provider is an open input. More importantly the address schema is unspecified, and this market needs подъезд (entrance), этаж, and ориентир (landmark) as structured fields plus a legitimate… |
| Operator-created customers with no self-service registration event | 0015 (consent model) | **Partial** — The rule 'absence of a decision is not consent' handles it correctly, so a staff-created customer is simply non-contactable until they consent. What is missing is the same treatment for bul… |
| Customer identity: phone-authenticated accounts, principal links, brand profiles, no auto-merg… | 0015 + 0029 | Complete |
| Brand catalog authoring, immutable publication, storefront reads only from the snapshot, locat… | 0016 | Complete |
| Product typing (parent/variant/simple/modifier), shared reusable modifiers, per-variant modifi… | 0016 | **Partial** — Parent product, variants, and brand-owned reusable modifier groups are all present. Absent: combo/bundle products with choice-sets and per-variant price maps, nested modifier variants (dept… |
| Fiscal product classification on catalog nodes (ИКПУ/MXIK, package code, VAT, marking, alcohol… | 0016 | **Partial** — An open input ('SPIC/unit/VAT classification semantics') with the legacy fields explicitly not copied. In practice these are mandatory per dish in Uzbekistan and aggregators reject menus wi… |
| Catalog merchandising and reference data (tags, recommendations, attributes, ingredients, depa… | 0016 (legacy merchandisin… | **Partial** — Explicitly listed as requiring a product MIGRATE/TRANSFORM/ARCHIVE/RETIRE disposition before catalog cutover — named, not decided. Departments/kitchens matter most because they are the prod… |
| Inventory: append-only movement ledger, transactional position, expiring reservations matched… | 0017 | **Partial** — The stop-list is exactly this model's BINARY mode with a movement reason, and QUANTITY is decided though unbuilt. Two Delever behaviours do not fit: a per-day portion allowance that auto-re… |
| Deterministic pricing, price books scoped by brand/location/channel, VAT-inclusive tax extract… | 0018 | Complete |
| Promotions, promo codes, coupon redemption ledger with per-customer and global limits under co… | 0018 | **Partial** — Decided but unbuilt, and the rule vocabulary is narrower than Delever's. Missing condition/action types: markup (наценка — the same engine raising price), geozone polygon condition, named-c… |
| Order lifecycle: statuses, transitions, immutable commercial snapshot, status timeline, approv… | 0019 (+ docs/domains/stat… | **Partial** — Canonical states are fixed and code-owned, which is right. Missing: the per-order status timeline is an operator-facing surface in Delever (Время по статусам), not only audit evidence; canc… |
| Scheduled / pre-orders (Предзаказ, Future state, lead time, out-of-hours branch choice) | 0019 (Scheduled/pre-order… | **Partial** — Lead time, reprice-on-wait, timezone and DST handling are decided. Not decided: the separate kitchen-release instant (Delever's kitchenSentTime / editable fire time) distinct from confirmed… |
| Checkout idempotency, one-order-per-retry, no partial reservation, order version compare-and-s… | 0019 + 0031 | Complete |
| Transactional notifications: versioned templates with locale fallback, allowlisted typed varia… | 0020 | Complete |
| Provider-preapproved SMS templates (Eskiz/Playmobile operator moderation) | 0020 | **Partial** — The alternatives table retains provider-managed templates 'where a regulator or gateway requires preapproved templates' and references the approved external template. The approval-state wor… |
| Order status trigger messages (Триггеры → Статусы заказов) with per-channel, per-order-type, p… | 0020 + 0019 events | **Partial** — Templates keyed by (class, channel, locale) exist; keying additionally by order type and source channel does not, and Delever's ~13 interpolation variables include courier first name, surna… |
| SaaS plans, versioned entitlements, per-resource limits with HARD/SOFT/METER_ONLY enforcement,… | 0021 | **Partial** — Delever's heterogeneous billing units (per brand, per branch, per kiosk unit, per courier service, one-off) fit plan_entitlements plus usage dimensions. Not decided: a prepaid tenant wallet… |
| Legacy migration: strangler by entity and journey, one authoritative writer per capability, bu… | 0024 | Complete |
| Fine-grained authorization: capabilities as resource.action, grants at platform/tenant/brand/l… | 0025 (+0003, 0021) | Complete |
| Permission-gated navigation plus module locks (Delever's two orthogonal gates) | 0025 + 0021 | Complete |
| Provider installations bound per brand/location, secret references, capability discovery and s… | 0026 (+0011) | Complete |
| Audit evidence: immutable, transactional, structured before/after, required reason, actor is t… | 0027 | Complete |
| PII protection: field classification, envelope encryption with tenant-scoped keys, keyed-hash… | 0029 + 0034 | **Partial** — Courier precise-location history is explicitly named PERSONAL_SENSITIVE, which is the right class. Retention periods per class and the lawful basis for continuous employee location telemetr… |
| Scoped configuration and policy resolution (location → brand → tenant → platform), typed code-… | 0030 | Complete |
| Delever's very large settings surface (base order rules, delivery settings, catalog switches,… | 0030 | **Partial** — The mechanism is right and covers almost all of it. Each policy's *content* belongs to its capability ADR, and three of Delever's knobs are design-forcing rather than configuration: courier… |
| HTTP conventions: Problem Details with stable codes, mandatory Idempotency-Key, expected-versi… | 0031 | Complete |
| Whether an in-flight order may be partial-patched (Delever exposes PUT with any subset of fiel… | 0031 | **Partial** — ADR 0031 mandates expected-version and intent-named commands, which answers the concurrency half. The financial half — what add-items or change-payment does to a quote, a reservation, a fis… |
| Event contract governance and topic policy | 0032 | **Partial** — Covers domain events. Behavioural/product-analytics events (visit, register, add-to-cart) are a different class with different volume, consent and retention, and are unowned — they power bo… |
| Caching and rate limiting: in-process first, Valkey only where shared invalidation is required… | 0033 | Complete |
| Frontend platform: Angular everywhere on web, storefront retained and re-pointed, operations r… | 0035 | **Partial** — Enumerates four web surfaces. Three more exist in this market and are not placed: the Telegram bot and Mini App (Delever's highest-volume channel), a kitchen display client with different a… |

### Capability areas needing a new decision

| Proposed ADR | Priority | Covers |
|---|---|---|
| Sales channels and location serviceability | blocks-pilot | Sales channel registry (Каналы продаж); per-channel payment-method and order-type matrices (Настройки контента); per-channel menu item enablement; branch working hours and delivery hours; non-working-day overrides; time-of-day pr… |
| Delivery zones, tariffs, and delivery-fee resolution | blocks-pilot | Delivery zones with tariffs; branch geozones; regions with geocoder bounding boxes; distance-banded and peak-hour delivery pricing; minimum basket for delivery; free-delivery threshold per zone; road-vs-radius distance mode; use-… |
| Legal entities, fiscal receipts, and fiscal product classification | blocks-pilot | Manual fiscalization retry; per-branch INN; fiscalized payment-type selection; VAT percentage; ИКПУ/MXIK and package code on products, variants, modifiers and the delivery fee; marked goods (Data Matrix) and per-unit line expansi… |
| Operator-assisted ordering, order amendment, and terminal outcome acc… | post-pilot | Manual order creation by an operator; client lookup and auto-create by phone; add items to a live order; change payment type after creation; edit address on a live order; cancellation reason registry with dual internal/customer-f… |
| Marketplace channel: inbound aggregator orders and the partner API | post-pilot | Uzum Tezkor, Yandex Eda, Wolt, Express24, Chocofood, Click Mini-App integrations; dynamic/generic aggregator onboarding; partner OAuth API (restaurants, availability, order CRUD, menu composition/availability/promos); external or… |
| Kitchen execution, production routing, and kitchen release | post-pilot | Kitchen orders queue partitioned by fulfilment type; buffer/hold queue; VDU display; expo/handover station; stop-list operation from the kitchen; branch open/close from KDS; courier assignment and third-party dispatch from KDS; p… |
| Courier compensation, shifts, and settlement | post-pilot | Courier types by vehicle and distance band; courier fares (fixed + per-order + per-km); bonuses and penalties; attendance and work schedules; shift enforcement on order acceptance; courier balance and billing mode; salary report… |
| Reporting, analytics, and the metric layer | post-pilot | Dashboard tabs and shared filter bar; order/branch/operator/courier reports; SLA time-bucket reports; external delivery cost reconciliation (charged vs provider-billed); promo-code redemption reports; ABC/XYZ product analysis; de… |
| Marketing campaigns, audiences, and engagement content | post-pilot | SMS/push/Telegram broadcast campaigns; RFM and registration-source targeting; birthday campaigns; customer inactivity and cart-abandonment triggers; referral links and Telegram Mini App deep links; unique per-recipient promo code… |
| Real-time operational push and field telemetry | post-pilot | Live order queue and status counters; live operations dashboard; dispatcher board with drag-to-assign; courier live map with 10-second refresh; courier device telemetry; kitchen display refresh and offline tolerance; customer-fac… |
| Loyalty, stored value, and split tender | later | Cashback/bonus point balance with expiry; customer deposit accounts; cashback and deposit as payment types; split tender; accrual and redemption rules; balance-change notifications; bonus-paid amount visible to the courier |
| Dine-in: table service, reservations, and QR ordering | later | Table reservations with external reservation ID; sections and tables; multi-table bookings; QR dine-in menu in view-only, settle-existing-bill, and full-ordering modes; hall as a fulfilment type and a sales channel |

### Deliberately not building

| Delever capability | Why not |
|---|---|
| Tenant-configurable order statuses and a reorderable state machine (Delever stores status as a… | A tenant-editable state machine makes ADR 0032 event contracts, ADR 0019 process managers, every status-driven automation, and every cross-tenant report ungovernable — the same status name would mean different things in two tenants. Qoida already fixed a canonical vocabulary in docs/domains/state-m… |
| Multi-branch orders (Delever's steps[], where one order carries several branches each with its… | Two nested state machines and per-step money, for a customer behaviour — ordering from two kitchens in one basket — that no evidence in the inventory shows anyone actually doing. ADR 0019 deliberately binds a cart to one location and rebuilds on change, and ADR 0002 rejected extra hierarchy tiers o… |
| Accepting an externally computed discount on Qoida-priced channels (Delever's 'Свободная скидк… | It exists so aggregators can push an arbitrary discount amount the platform cannot re-derive. On an aggregator order that is unavoidable and belongs in the marketplace ADR behind an explicit externally-priced flag. Extending it to Qoida's own channels would destroy ADR 0018's central promise — that… |
| Recruiting module: vacancies and candidate pipeline | A mini-ATS inside a delivery platform. It collects name, phone and free-text CVs from anonymous public users, which is a distinct PII class with its own retention, consent and access obligations, in return for zero commercial differentiation — every tenant can already use a hiring form or a Telegra… |
| General-purpose editorial CMS with tenant-authored raw HTML (static pages, news, gallery, reci… | Delever's static-page editor accepts raw HTML, which is an XSS surface pointed at the tenant's own customers, and the whole area is website-builder work rather than commerce. Build instead the bounded, typed merchandising slots the storefront genuinely needs — banner, story, promo card, each with a… |
| Telephony: embedded softphone, PBX integration, call records, and operator answer-speed KPIs | An entire integration category (OnlinePBX, Asterisk) that shares nothing with POS, payments, delivery or messaging adapters, plus call recording with its own consent and retention regime. Delever's own operator KPI report showed 'no data' in the documented instance, which suggests it is not widely… |
| Ingredient-level bill of materials, technological cards, and recipe costing | The parity brief assumed Delever has this; the inventory analysis establishes it does not — its Ингредиенты is a customer-facing localized composition label and its Рецепты is marketing content, and depletion lives in the POS (iiko, 1C) that Delever integrates with rather than replaces. ADR 0017 al… |
| AI generation of fiscal classification codes (Delever AI-generates ИКПУ with a liability discl… | A wrong ИКПУ is a tax classification error on a legal receipt, and generating it automatically transfers that risk from the tenant to Qoida while making it invisible. AI assistance on descriptions, weights and nutrition is harmless and worth having; on a tax classifier it is not. If any assistive s… |
| Per-module V1/V2 interface version toggles as a tenant-visible product feature | Genuinely clever for Delever, which is strangling a live product with paying tenants and has to let each migrate on its own schedule. Qoida is building the new system, and ADR 0024's single-authoritative-writer-per-capability gate already delivers staged, per-tenant cutover without shipping and mai… |
| Prepaid tenant wallet with expiring credit and debt-triggered feature restriction | Delever requires a $500 deposit, funds a wallet from it, and expires the balance. Expiring prepaid credit is a customer-hostile financial product with real accounting weight (a liability that vanishes) for a benefit — cash collection — that an invoice plus ADR 0021's existing PAST_DUE→SUSPENDED lif… |
| Self-service kiosk hardware integration (Arcus2 bank terminals, 58/80mm receipt printers, Data… | A hardware product line with device provisioning, certification, field support and a per-device fiscal identity, gated on physical terminals in restaurants. Kiosk should remain a recognised sales channel in the channel registry from day one — that costs a row — while the device integration waits un… |
| Delever and legacy security anti-patterns: base64(login:password) as a partner client secret,… | Recording these as explicit non-goals matters because they are the shape of the systems being replaced and will be proposed as 'how it worked before'. ADR 0026 already closes tenant-supplied endpoints at the model level and ADR 0028 owns credentials; partner onboarding must issue rotatable OAuth cl… |

## Legacy Qoida applications

The three archived applications, as the second parity input. What they do is what the client's staff and customers already expect.

| Application | Screens | Domain vocabulary |
|---|---|---|
| qoida-dashboard (package name `rayhon-dashboard`) — archive… | 17 | company (компания) — the brand / parent entity. Has i18n name+description, slug, logo, background image. Owns vendors. The order-board filter hardcodes five: rayhon, marmar, jizbiz, kids_plate, and '… |
| Qoida Storefront "JizBiz" (qoida-storefront-jizbiz) — singl… | 30 | Customer account — identified by Uzbek phone number ('998' + 9 digits), authenticated by OTP; JWT payload carries sub, role_id, phone_number, exp, iat. Profile is first_name, last_name, language, ima… |
| Milliy (Xcode target "Rayhon" / "Rayhon Birzumda") — the le… | 35 | vendor / branch (BranchModel: id, name, phone, active, pre_order, start, finish, error) — one per app install, delivered inside the cart response, menu composition (UIElementModel: category rail, off… |

---

## Full capability inventory by section


### Заказы (Orders)

Orders is Delever's operator cockpit: a filterable, tab-by-status order list with bulk actions, a per-order "..." action menu that drives the whole order lifecycle (edit, add items, change payment, assign or call a courier, kitchen comment, print to POS, fiscalize, complete, cancel), plus four sibling tools — table reservations, re-issuing a payment invoice, a live courier map, and manual order creation. The order itself is a multi-branch document (`steps[]`) with an array of payments, a status-note timeline, a delivery type of self-pickup/delivery/aggregator/hall, and a fiscalization URL. Critically, the docs for this section are largely video-only: the state machine, list columns and filters are only recoverable from the reports/settings/API pages, and the real state machine is Future → Now → Operator Accepted → Courier Accepted → Vendor Accepted → Vendor Ready → Courier Picked up → F…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Order list table (Таблица заказов / Список заказов) | The operator's main working surface: a paginated table of every order across the tenant with per-row action menus. Explicitly documented as handling externally-delivered orders (Yandex, Wolt, Millenium) alongs… | operator, manager |
| ● | Status tabs on the order list (Вкладка со статусами заказов) | The order list is segmented into tabs by order status so an operator can work one queue at a time (new/unaccepted, in progress, finished, cancelled). Introduced with the Admin V2 orders module rebuild in May 2… | operator |
| ● | Order list filters (Фильтры списка заказов) | Narrows the list by a calendar period plus branch, aggregator, source/sales channel, delivery type, courier, and payment type. The same filter vocabulary is reused across the order reports. A July 2026 fix spe… | operator, manager |
| ● | Order detail card (Карточка заказа) | Opened by clicking an order ID. Shows the client's name and phone and the delivery address, the item composition with quantities and per-item prices and modifiers, payment details and paid/unpaid state, the am… | operator, support |
| ● | Multi-branch order steps (steps[]) | An order is not bound to one branch: it carries an array of steps, each step being a branch with its own address, phone, menu, its own product lines, its own step amount, and — importantly — its own status. A… | operator, kitchen |
| ● | Order status timeline / status notes (status_notes, «Время по статусам») | Every status the order passes through is recorded with a timestamp, producing an auditable lifecycle trail. The dashboard renders it as time-spent-per-stage, and the 'Все отчеты' order report exposes the per-s… | operator, manager |
| ● | Order state machine (Статусы заказа) | The documented lifecycle is: Future (scheduled pre-order) → Now → Operator Accepted (Оператор принял) → Courier Accepted (Курьер принял) → Vendor Accepted (Филиал принял) → Vendor Ready (Филиал подготовил) → C… | operator, courier, kitchen,… |
| ● | Change payment type on an existing order (Поменять тип оплаты) | An action in the per-order '...' menu that switches the order's payment method after the order exists — e.g. a customer who intended to pay online decides to pay the courier in cash. | operator |
| ● | Add items to an existing order (Добавить заказ) | Lets an operator append products to an order that has already been placed — the classic 'the customer called back and wants to add a dessert' flow — rather than forcing a second separate order. | operator |
| ● | Edit an existing order (Изменить заказ) | Full edit of a live order: client details, delivery address and its detail fields, items and quantities, comments, payment method, delivery type and pre-order time. | operator |
| ● | Assign a courier (Назначить курьера) | Attaches a specific in-house courier to the order from the order's action menu. Once assigned, the courier sees it in the Delever Courier app under 'Мои заказы' and drives the order's status forward from their… | operator, dispatcher |
| ● | Call an external courier (Вызвать курьера) | When no external delivery service is pre-selected on the order, the action menu offers a generic 'call a courier' that dispatches the request to the configured external delivery services. Can be automated syst… | operator, system |
| ● | Millenium delivery: create, attach after the fact, and cancel (Миллениум дост… | The most fully documented courier-provider flow. At creation, if the order type is Доставка, Millenium becomes selectable as the delivery service and shows an ESTIMATED delivery price; on save, if Millenium's… | operator |
| ● | Yandex Delivery from the order menu (Яндекс доставка) | Dispatches the order to Yandex's courier aggregator for venues without their own couriers. As of 2026 the order list, order card, website and KDS all display the external courier's estimated arrival time, and… | operator |
| ● | Complete the order (Завершить процесс) with completion reasons (Причины завер… | Closes the order as done. The reason is chosen from a tenant-managed registry of completion reasons — examples given are 'Доставлен', 'Забрали заказ', 'Доставлен сторонней службой', 'Самовывоз выполнен'. The r… | operator |
| ● | Cancel the order (Отменить заказ) with reason and write-off type (Причины отм… | Cancels the order against a tenant-managed cancellation-reason registry. Each reason carries TWO texts — an internal name for operators ('Не дозвонились', 'Клиент передумал', 'Курьер опоздал', 'Из-за отсутстви… | operator |
| ● | Manual fiscalization (Фискализировать) | An action in the per-order '...' menu that manually pushes a paid order to the tax fiscalization service when the automatic attempt failed — documented for Click-paid orders. Admins can complete fiscalization… | operator, admin, accountant |
| ● | Table reservations (Бронирования) | A full table-booking module inside Orders. Creating a booking captures client name and phone (plus an optional secondary phone), an optional comment, the branch, the date and a from-time/to-time interval, then… | operator, host |
| ● | Manual order creation by an operator (Создать заказ) | The call-centre order entry flow. The operator searches for the client by phone number; if not found, they type a name and the client record is created automatically. An 'history' icon next to the phone opens… | operator |
| ● | POS integration error surfacing on order creation (Ошибки интеграции с POS) | When an order fails to reach the POS, the failure is shown to the operator at creation time with the specific cause — unmapped/unlinked products, unmapped payment types, or a product that is inactive in the PO… | operator |
| ● | Order status trigger messages (Триггеры → Статусы заказов) | Automated customer messages fired by order status transitions. Each trigger binds a status (e.g. 'Филиал принял', 'Готовится', 'Курьер в пути', 'Завершен') to a localised message template in Russian, English a… | marketing, admin (configure… |
| ● | Auto-accept incoming orders (Автопринятие заказа) | Moves incoming orders straight to 'Принят' without an operator touching them. Configurable per source channel (e.g. only Telegram bot and website), and gated by a minimum count of the customer's previous succe… | admin (configures), system… |
| ● | Base order-processing rules (Базовые настройки заказов) | Tenant-wide policy governing every order: VAT percentage; fiscalization INN; which payment types Delever fiscalizes; a payment-notification integration token; automatic sending of a payment link to the custome… | admin |
| ● | Sales channel registry (Каналы продаж / Типы заказов) | A tenant-managed CRUD registry of the channels orders arrive through. Each row has a display name, a system platform type, an active/inactive status and edit/delete actions, and the list is searchable. Documen… | admin |
| ● | Payment type registry (Типы оплаты) | A tenant-managed registry of payment methods. Each entry has a localised customer-facing name (ru/en/uz), a 1:1 icon up to 1 MB, an active flag, an optional bound acquiring integration (Payme, Click, Atmos, Uz… | admin |
| ● | Order-level pre-order / scheduled delivery (Предзаказ) | An order can be placed for a future time rather than ASAP. The order carries a pre-order time and a Да/Нет pre-order flag; the state machine has an explicit 'Future' state preceding 'Now'. If a pre-order is pl… | operator, customer, system |
| ◐ | Order search including external aggregator IDs (Поиск по ID агрегаторов в спи… | Free-text search over the order list that resolves not only Delever's own order ID and the client's phone number, but the external order ID issued by the aggregator — explicitly Yandex Eats, Wolt, Glovo and Uz… | operator, support |
| ◐ | Bulk order selection and bulk courier assignment (Массовое назначение курьера… | Checkboxes on the order list let an operator select many orders at once and assign them all to a single courier in one action, then save. Built to stop operators assigning couriers order-by-order during peaks. | operator, dispatcher |
| ◐ | Late-order detection and highlighting (Заказ опаздывает с / Индикатор опаздыв… | A tenant-configured threshold in minutes after which an in-flight order is flagged as late; the row is then colour-highlighted in the list using an admin-chosen colour (e.g. red). Backed by a dedicated 'По опо… | operator, manager |
| ◐ | Drafts / abandoned carts tab (Заказы → Черновики) | A tab inside the Orders section listing carts that were started but never converted into an order, with a total count and a breakdown by the sales channel the cart was started in, so the business can see which… | manager, marketing |
| ◐ | Kitchen comment (Комментарий к кухне) | Attaches a free-text instruction from the operator to the kitchen for this specific order, separate from the customer's own order comment and separate from per-product comments. | operator, kitchen |
| ◐ | Print to POS (Печать) | Pressing 'Печать' does not print locally — it sends a print request to the integrated POS system, which then produces the ticket/receipt. | operator, kitchen |
| ◐ | Re-issue a payment invoice (Выставить счет) | Handles the case where the customer never received the payment request on their payment system, or their payment-system account is registered to a different phone number than the one on the order. The operator… | operator, support |
| ◐ | Live courier map (Показать на карте) | A live map of all couriers using the Delever Courier app. For each courier the operator sees their name, how many orders they are currently carrying, and their phone's battery level. Couriers can be filtered/v… | operator, dispatcher |
| ◐ | Operator order-creation settings (Настройки создания заказов в админ-панели) | Configures what operators can do when hand-entering orders: which payment types and which order types are offered in the operator UI, plus three toggles — whether to offer a 'customer wants a callback from an… | admin (configures), operato… |
| ◐ | Manual creation of aggregator orders (Ручное создание заказов агрегаторов) | Even though Delever integrates with several food aggregators, operators can also create an aggregator-type order by hand — for a partner without an API integration, or to recover an order that failed to sync. | operator |
| ◐ | Yandex pickup-code verification at handover (Код выдачи заказа) | Prevents an order being handed to the wrong courier. Staff search by Yandex order ID, the system loads the order (Yandex Eats ID + Delever ID, payment type and amount, client info), and tapping the order opens… | kitchen, branch staff |
| ◐ | Order reports and export (Отчёты по заказам) | Two report families over the order data. 'Все отчеты' is a logistics/time audit — order ID, branch, total lifetime, and the timestamps or durations for operator-accepted, branch-accepted, branch-prepared and c… | manager, finance |
| ○ | Live operations dashboard counters (Лайвтайм Дашборд для операторской) | A shift-supervisor screen with two large real-time counters: 'В процессе' — orders currently in active work, explicitly spanning operator confirmation, kitchen preparation and courier transit — and 'Отменено'… | manager, shift supervisor |

**Open questions this section raises**

- Delever stores order status as `status_id` (a UUID) rather than a fixed enum, and the branch-first/courier-first setting reorders the lifecycle. Does Qoida want a fixed, code-owned order state machine (simpler, testable, ADR-friendly) or tenant-configurable statuses (matches Delever, but makes ADR…
- Does a Qoida order have one fulfilling location or many? Delever's `steps[]` gives each step its own branch, menu, product lines, amount AND status — two nested state machines. Retrofitting this later is very expensive, so it must be decided before ADR 0019 is written.
- Cancellation in Delever posts a financial/inventory write-off ('со списанием' vs 'без списания'). Qoida ADR 0017's inventory ledger is BINARY-only and ADR 0013 is unstarted — who owns the write-off posting, and does a cancelled order need to consume stock?
- Fiscalization has no ADR at all. Uzbek law requires fiscal receipts (INN per legal entity/branch, per-payment-type fiscalization, a fiscalization URL on the order, manual retry from the operator UI). Is this a new ADR, part of ADR 0013 payments, or a provider capability under ADR 0026?
- Table reservations require branches to have sections and tables as first-class entities with time-interval availability — nothing in ADR 0016 covers floor plans. Is reservations in scope for parity, and if so does it belong to the order aggregate or a separate one? (Delever puts it under Orders and…
- Delever supports simultaneous multi-provider courier dispatch ('несколько такси-сервисов одновременно') and courier order grouping (multiple orders in one courier run with a merge radius in metres). Does ADR 0014's sourcing model cover racing providers, cancelling the losers, and batching orders in…
- Payment types include Кешбэк (loyalty points) and Депозит (stored value) as first-class methods, and the order carries a `payment[]` array — so split tender is representable. Is split tender in scope for Qoida, and are loyalty/deposit modelled as payment methods or as discounts? (ADR 0018 is built…
- The address model needs подъезд (entrance) and ориентир (landmark) as structured fields for this market. Has Qoida's address schema accounted for these, and for delivery orders that legitimately have no geocoded coordinates?

### Дашбоард (Dashboard) and Клиенты (Clients)

Delever's Dashboard is a read-only analytics surface split into tabs — Заказы (Orders), Клиенты (Clients), Продукты (Products), Сотрудники/Топы (Employees/Leaderboards), Отчёты (Reports), Отчёты по курьерам (Courier reports), Агрегатор (Aggregator health), and a Лайвтайм (live) dashboard for the call-centre floor. It computes operational KPIs (revenue, order counts, average check, cook/delivery/pickup timings, distance), channel and payment mix, an SLA time-bucket breakdown per branch and per courier, ABC/XYZ product classification with a ClickHouse-backed sales forecast, a Tashkent order heatmap, and an integration-error log. The Клиенты area is a CRM: a searchable customer list with status and per-customer actions, CSV import/export, an RFM segment builder, a birthday calendar for loyalty campaigns, cohort/repeat-purchase analytics (LTV, order frequency, basket depth, new-vs-returning…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Dashboard tab structure (Дашбоард) | The dashboard is a multi-tab analytics workspace, not a single page. V1 exposes four tabs — Заказы, Клиенты, Продукты, Сотрудники. The V2 (BETA) rebuild expands this to eight — Заказы, Клиенты, Топы, Продукты,… | manager, owner, operations… |
| ● | Global dashboard filter bar (Глобальная панель фильтрации) | A filter bar sits above every dashboard tab and is applied consistently across all widgets below it. Filters are: Дата/Время (date range), Группировать по (grouping granularity — day/month/year), Тип оплаты (c… | manager |
| ● | Orders KPI counters (Счетчики: Доход, Кол-во заказов, Средний чек, тайминги) | Top-of-page numeric tiles for the selected period: Доход (revenue), Кол-во заказов (order count), Средний чек (average check), Общая сумма заказов (gross order total), and operational timings in minutes — Сред… | manager |
| ● | Sales funnel by order status (Воронка продаж) | A funnel chart breaking the selected period's orders down by final status and by fulfilment type (доставка, самовывоз, агрегатор, в зале, внешняя доставка). Clicking a cancelled slice reveals the cancellation… | manager, operations director |
| ● | Order source / channel mix charts (Источники заказа, Доля источников) | Pie charts showing share of order count and share of revenue by acquisition channel, plus a bar chart of average check per channel. Documented channels: Admin panel (operator-entered phone orders), Telegram Bo… | manager |
| ● | Live operations dashboard for the call centre (Лайвтайм Дашборд для операторс… | A wall-board view for the shift supervisor. Two oversized real-time counters: В процессе (orders currently in active work — awaiting operator confirmation, cooking, or with a courier) and Отменено (cancellatio… | operator, shift supervisor |
| ● | Daily operations report (Общий отчет — посуточная динамика) | A per-day table of operating metrics for the selected period. Columns: Дата, По общей цене (gross revenue before discounts/adjustments), По цене продукта (net food value), Отмененные заказы (cancellation count… | manager, finance |
| ● | External courier cost reconciliation (Отчеты по курьерам — внешняя логистика) | Verifies settlement with third-party delivery services by putting three money figures side by side per order: Сумма заказа (what the customer paid for food), Сумма доставки (delivery fee charged to the custome… | manager, finance |
| ● | Customer list (Список клиентов) | The core CRM grid. Lists registered customers with name, phone number, and account Статус (e.g. Активный / active-or-not). A search bar at top-left filters by name or other fields. Each row has a three-dot act… | operator, manager |
| ● | RFM segment builder (Сегменты / RFM) | An interactive segmentation tool over the whole customer base. Filters: period, Recency (days since last order), Frequency (number of orders), Monetary (total order value), and Источник регистрации (bot, admin… | manager, marketing |
| ● | Customer KPI tiles (Ценность клиента, Частота заказов, Глубина чека, LTV) | Headline customer-economics metrics with explicit definitions given in the docs: Общее количество новых клиентов (unique customers who made a first purchase or registered in the period); Глубина чека (basket d… | manager, owner |
| ● | Repeat-purchase and cohort analytics (Анализ повторных продаж) | Four linked retention views: Распределение клиентов по количеству заказов (bar chart of how many customers made exactly 1 order, 2, 3, up to loyal 10+ customers); Динамика повторных заказов по времени (histogr… | manager, owner |
| ● | Segmentation CRM table (Модуль Segmentation / таблица Clients) | The V2 rebuild of the segment builder, sitting inside the Clients dashboard. Filters the base by Источник регистрации, Количество заказов, Дни с последнего заказа, and Сумма заказов, producing a table with nam… | manager, marketing |
| ◐ | Payment type mix (Доля видов платежей в выручке / Типы платежей) | Shows what percentage of total revenue each payment method accounts for. Documented values are cash and online, with online further resolvable to the specific gateway. | manager, finance |
| ◐ | Review / satisfaction analytics (Отзывы) | A chart showing the split of positive versus negative ratings across four separately-scored survey categories — Meal (food), Operator, Courier, and Delivery time — for each time interval, expressed as a share… | manager |
| ◐ | Order heatmap over Tashkent (Тепловая карта) | A geographic heatmap visualising order density across the city, using red and green zones to show concentration. A separate map view plots today's orders as pins; clicking a pin reveals the client name and ord… | manager, operations director |
| ◐ | Integration error log (Ошибки интеграции / Частота ошибок) | Two tables on the orders dashboard surfacing technical failures of APIs and plugins — the frequency of errors and the specific integration errors — with columns for time and count, so a manager can correlate a… | manager, admin |
| ◐ | Aggregator liveness monitor (Отчёт по последнему заказу каждого агрегатора) | A matrix of branches × aggregator channels (Uzum Tezkor, Yandex Eats) where each cell shows the date and time of the last successful transaction through that channel for that branch. A stale timestamp is the s… | manager, admin |
| ◐ | Branch leaderboard (Лучшие филиалы) | Ranks branches within the network by performance for the period. Columns: Название филиала, Количество заказов, Кол-во доставок / Кол-во самовывозов / Кол-во агрегаторов (channel split of fulfilled orders), До… | manager, owner |
| ◐ | Operator leaderboard (Лучшие операторы) | Ranks call-centre operators by orders taken and average check for the period, explicitly framed as measuring an operator's up-sell ability. Automated gateways (bot, website, aggregator channels) appear in the… | manager |
| ◐ | Courier leaderboard (Лучшие курьеры) | Ranks couriers by orders delivered, with per-courier revenue carried and total mileage in kilometres. Documented uses are payroll calculation, bonus accrual, and assessing vehicle wear and fuel-consumption eff… | manager, operations director |
| ◐ | Daily summary reports (Ежедневный отчет 1 и 2) | Two consolidated roll-ups at the foot of the reports page. Report 1 aggregates the whole selected period by order type, showing count and money, with a Сумма с учётом доставки column that includes the logistic… | manager, finance |
| ◐ | Per-order SLA report (Отчет по заказам) | An order-level log for SLA control: ID заказа (unique receipt number), Название филиала (which kitchen prepared it), Общее время (actual elapsed time from order creation to completion), and Оператор принял (wh… | manager |
| ◐ | Full order audit log (Все отчеты по заказам) | The maximally-detailed per-order log used to adjudicate disputes. Carries customer data (Имя, Номер телефона), execution parameters (ID заказа, Оператор, Филиал, Тип доставки, Предзаказ [Да/Нет], Статус), and… | manager, support |
| ◐ | Branch SLA time-bucket report (Отчет по филиалам по времени) | Splits each branch's orders into fixed elapsed-time buckets — До 30 мин, 30-35, 35-40, 40-50, 50-60, Более 60 мин — showing both the order count and its percentage of that branch's total volume. Described as t… | manager, operations director |
| ◐ | Courier tariff audit report (Отчет по сумме доставки) | For each courier, breaks their deliveries down by the delivery fee charged, revealing which tariff zones they worked. Columns: Имя (courier, with nested per-tariff detail rows), Сумма доставки (fee for one ord… | manager, finance |
| ◐ | Courier SLA report (Отчет по курьерам по времени) | Same six fixed time buckets as the branch SLA report, but per courier, with count and percentage of that individual's personal volume in each bucket plus an Общий total column. Filterable by date range and by… | manager, operations director |
| ◐ | Promo code usage reports (Отчеты по промокодам) | Three related views on coupon programme effectiveness. Report 1 is a summary by promo-code hash identifier (discount_id) showing how many times each promotion was redeemed and the total discount value granted.… | manager, marketing |
| ◐ | ABC product analysis (ABC аналитика по продуктам) | Classifies every product into group A, B or C by contribution to revenue using cumulative Pareto shares — A is roughly the top 80% of sales value (the critical products), B the next ~15%, C the remaining ~5% (… | manager, owner |
| ◐ | XYZ demand-stability analysis (XYZ-аналитика) | Classifies products by demand predictability rather than value. Columns: Count, Total sales (so'm), Avg sale, STDEV (standard deviation of demand), Coeff. Variation (coefficient of variation), and the assigned… | manager, owner |
| ◐ | Customer list header statistics (Статистика по клиентам) | A strip of counters above the customer list: total number of customers, how many of them placed an order today, and how many registered today. Framed as an at-a-glance base-health check for the operator floor. | operator, manager |
| ◐ | Bulk customer import (Импорт) | An Импорт button in the top-right of the customer list that bulk-loads a customer base from a CSV or similar file. The imported records are traceable afterwards: 'import' appears as a distinct value in the reg… | admin, manager |
| ◐ | Manual customer creation (Добавить) | An Добавить button allowing staff to create a customer record by hand, for example when an operator takes a phone order from someone who has never used a self-service channel. | operator, manager |
| ◐ | Customer export (Скачать) | A Скачать button that exports the currently-filtered customer segment to CSV, so a marketing team can feed it into an external campaign tool. | manager, marketing |
| ◐ | Acquisition-source chart and registration/order trend (Кол-во клиентов по ист… | A pie chart of customers by acquisition channel — explicitly yandex-eats, admin-panel, bot (Telegram), uzum-tezkor, website, app (mobile), import — plus a line chart overlaying daily registrations (count) on d… | manager, marketing |
| ◐ | Customer funnel tracking (Отслеживание клиентов) | A day-by-day product funnel table tracking the classic user path: Входы (sessions/visits) → Зарегистрировано (registered) → Добавляет в корзину (added to cart) → Заказано (ordered). The documented use is findi… | manager, product |
| ◐ | Birthday loyalty module (Дни рождения / Клиенты с др) | An automatically maintained list of customers whose birthday falls in a chosen window. Shows Имя, Номер телефона and День рождение. A Дни рождения (День и Месяц) filter selects today or the coming week, so the… | manager, marketing |
| ◐ | Client inactivity trigger (Триггеры — Активность клиентов) | An automated re-engagement rule adjacent to the Clients area: it detects that a user opened the Telegram Bot or Mini App, did not complete a key action, and after a configured delay sends them a message. Confi… | manager, marketing |
| ◐ | Customer app profile capabilities (Профиль клиента в приложении) | The customer-facing counterpart to the admin CRM record. The app profile lets a customer edit personal details, manage delivery addresses (управление адресами), view cashback and promotions (просмотр кешбека и… | customer |
| ○ | Delivery-time and distance cohort charts (Когорта по часам дня недели, срез в… | A day-of-week × hour-of-day heatmap of order volume, plus histograms of order counts bucketed by delivery duration and by delivery distance. Used to find peak-load windows and to understand the geographic spre… | manager, operations director |
| ○ | Kiosk sales report (Отчет киосков) | A reporting slot for sales made through self-service kiosk terminals. Present in the UI but shows 'Нет данных' in the documented instance. | manager |
| ○ | Operator KPI report (Отчет по операторам — общие сведения) | A call-centre staff KPI block. Documented as displaying, when populated, the number of calls handled, answer speed, and the conversion rate of accepted enquiries into successful orders. Showed 'Нет данных' in… | manager |
| ○ | Product sales forecast (Product prediction — ClickHouse) | A chart overlaying an algorithmic forecast line (План, blue) against actual daily sales (Факт, red) per product, filterable to an individual product. The module is explicitly named after ClickHouse. | manager, owner |
| ○ | RFM cross-metric views (M-F, R-M, R-F) | Composite RFM projections on the Clients dashboard: M-F plots total order value against order frequency; R-M shows days since last purchase; R-F shows order count. These give a two-dimensional read of the base… | manager, marketing |

**Open questions this section raises**

- No blacklist feature is documented anywhere in the Clients area. The only suppression mechanism is the per-customer Статус (Активный) toggle. Qoida must decide whether it needs a real blacklist with a reason, an actor, an expiry, and defined enforcement semantics — does a blacklisted customer get b…
- Delivery address management appears only in the customer-facing app profile, never in the admin panel. Can an operator taking a phone order see, reuse, or edit a customer's saved addresses? For a call-centre-heavy market this is almost certainly required, and it changes both the customer model (add…
- Cashback (кешбек) is visible in the customer app but has no admin-side documentation — no balance ledger, no accrual rules, no expiry, no manual adjustment. Qoida must decide whether it is building a real loyalty/wallet ledger (which is a double-entry balance with its own consistency and audit requ…
- There is no documented per-customer detail page. The docs only describe an inline edit and a status change from the list's three-dot menu. Does a customer record have a full profile view with order history, addresses, complaints, refunds, notes and communication log? If so, that view is a much larg…
- RFM and LTV are presented as computed metrics with no stated definition of the period baseline, the currency handling, or whether cancelled and refunded orders are excluded. Qoida needs to fix these definitions explicitly, because two dashboards disagreeing on average check is a credibility problem…
- The entire dashboard is an OLAP workload — cohort heatmaps, ABC/XYZ with standard deviation, funnel-by-day, per-day per-branch per-channel matrices — and the competitor uses ClickHouse. Qoida has PostgreSQL only and no analytics/reporting ADR. Decide now whether this is materialized views in Postgr…
- Behavioural events (Входы, Зарегистрировано, Добавляет в корзину) are required for the funnel table and for the inactivity trigger, but they are client-side telemetry, not domain events. Qoida has no place for these. Decide whether they flow through the same Kafka contract governance as domain even…
- Customers acquired through aggregators (yandex-eats, uzum-tezkor) are counted in the restaurant's customer base. Aggregators frequently mask or proxy customer phone numbers. How does Qoida create or match a customer identity from an aggregator order, and what happens when the same human later regis…

### Персонал (Personnel)

Delever's Personnel section is a workforce module for two staff classes only: call-centre operators and in-house couriers. It holds six pages — Operator, Courier, Courier Type, Courier Fare, Attendance, Courier Bonuses/Penalties — but the capability it really delivers is a courier payroll engine: each courier is classified by a vehicle-based Courier Type, paid via a Courier Fare (fixed + per-order + per-km), adjusted by rule-driven bonus/penalty accruals against a personal courier balance, gated by an attendance/shift schedule, and settled through a "Зарплата" (Salary) report whose "К оплате" (To be paid) column is the final payout figure exported to accounting. Shift management is a policy toggle rather than a rostering product: "Проверять рабочий график курьеров" blocks a courier from accepting any order unless their shift is active on the schedule, and the courier mobile app shows th…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Operator (Оператор) | Creates and manages call-centre operator accounts — the staff who take phone orders and work the admin panel order queue. Operators get login credentials and are the unit of attribution across the whole order… | admin / manager creates; op… |
| ● | Courier (Курьер) | Creates an in-house courier record and binds that courier to one or more branches (филиалы). The courier record is the anchor for everything else in the module: it carries the courier type, the courier fare, t… | admin / manager creates; co… |
| ● | Courier Type (Тип курьера) | A classification record grouping couriers by vehicle and by the distance band they are eligible to serve. Used to decide which couriers are candidates for which orders and to segment reporting. Fields are: Наз… | admin / manager |
| ● | Courier Fare / Tariff (Тариф курьера) | The pay-rate record that drives courier earnings. Named tariff carrying: Тип тарифа (whether this tariff is a bonus or a penalty), Фиксированная сумма (fixed amount), Сумма за заказ (additional amount accrued… | admin / manager configures;… |
| ● | Delivery Tariff vs Courier Tariff separation (Тарифы доставки vs Тариф курьер… | Delivery tariffs price what the CUSTOMER pays for delivery (resolved by branch and by delivery zone, with delivery-zone tariff taking priority over branch tariff). Courier tariffs price what the COURIER earns.… | admin / manager |
| ● | Attendance (Посещаемость) | A per-courier attendance / work-schedule record, viewable filtered by courier, date and time. It is the input to shift enforcement and the source of the 'часы' (hours worked) column in the salary report. The c… | manager schedules and revie… |
| ● | Courier work-schedule enforcement (Проверять рабочий график курьеров) | A delivery-settings toggle that forbids couriers from accepting orders when their shift is not activated according to the work schedule. This is the actual shift-management mechanism: attendance is not merely… | admin configures; enforced… |
| ● | Courier billing mode (Включить биллинг для курьеров) | Master toggle that turns on financial accounting for couriers: it enables a personal courier balance and the deduction of commissions/fees from that balance for completed deliveries. When off, the courier app… | admin configures; courier a… |
| ● | Courier bonuses and penalties (Бонусы/Штрафы курьеров) | Rule-driven adjustments to the courier's balance. A bonus is awarded when the courier satisfies all configured conditions and money is ADDED to their balance; a penalty is applied when the courier violates the… | manager configures rules; f… |
| ● | Courier salary report — Зарплата tab (Отчёт по персоналу → По курьерам → Зарп… | The payout settlement view and the single most important artefact in the module. One row per courier with: Курьер (name), Заказы (completed deliveries), KM (total confirmed mileage), часы (hours worked), Вовре… | manager / finance / account… |
| ● | Dispatcher module (Диспетчерский модуль / режим диспетчеризации) | A dispatch mode on the Заказы → Показать на карте page for assigning orders to couriers at speed: drag an order card onto a courier's card to assign it instantly; click a courier card to filter the order list… | dispatcher / operator |
| ● | Courier GPS radius validation (Проверка действий курьера по радиусу) | Anti-fraud geofencing that stops couriers faking status changes from the wrong place. A master toggle plus two radii: Радиус, в котором курьер принимает заказ (max distance from the branch/pickup point within… | admin configures; enforced… |
| ● | Courier order-acceptance policy settings (Базовые настройки доставки — courie… | A cluster of policy toggles governing what a courier may see and take: Показывать курьерам только готовые заказы (courier only sees and can take orders the kitchen has moved to Готов); Локация клиента у курьер… | admin configures; enforced… |
| ● | Courier mobile app — authentication and order flow (Delever Courier App) | Courier signs in by first selecting the restaurant they are attached to, then username and password, with a remember-credentials option (a later release removed the mandatory restaurant selection). Home screen… | courier |
| ● | Courier mobile app — shift profile and earnings (Профиль) | The courier's own view of their shift: total hours worked, total orders completed and total kilometres driven for the shift, so they can track their own efficiency. If the restaurant has courier billing mode e… | courier |
| ● | Courier mobile app — daily / end-of-shift report | An in-app report page giving the courier the day's totals: Итоговая сумма заказов за день, a filter by payment method (cash, terminal, online, etc.), and per-order data including delivery distance, basket sum,… | courier; branch manager rec… |
| ◐ | Courier operational efficiency report — Курьеры tab (Отчёт по персоналу → По… | Per-courier logistics analytics: Кол-во заказов (deliveries completed), Сумма всех заказов (total basket value carried), Минимальное / Среднее / Самое длинное расстояние (shortest, average and longest single-t… | manager / operations direct… |
| ◐ | Courier delivery-sum report by tariff (Отчёты по курьерам → Отчет по сумме до… | Breaks each courier's orders down by the specific delivery tariff/zone price they were carried under: Сумма доставки (delivery price of one order in that tariff zone), Кол-во (how many orders at that price), О… | manager / finance audit |
| ◐ | Courier SLA time-bucket report (Отчёты по курьерам → Отчет по курьерам по вре… | Distributes every order a courier closed into delivery-duration buckets, showing both count and percentage of that courier's personal volume: До 30 мин (green/priority), 30–35 and 35–40 мин (standard acceptabl… | operations director / manag… |
| ◐ | Branch personnel report (Отчёты по персоналу → По филиалам) | Per-branch commercial and logistics audit. Channel-paired blocks of Количество / Средний чек for Админ (operator-taken), Бот (Telegram), Зал (dine-in), Приложение (mobile app) and Сайт; totals for Общие заказы… | manager / owner |
| ◐ | Operator performance report (Отчёты по персоналу → По операторам → Общие свед… | Per-operator call-centre KPIs: Количество заказов (confirmed receipts), Общая сумма (revenue brought in, national currency), Время обработки заказа (average time an order sat in that operator's hands — flagged… | manager / call-centre super… |
| ◐ | Employees dashboard — top operators and top couriers (Дашборд → Сотрудники) | A leaderboard view listing all restaurant staff (operators and couriers together) with a Top-10 operators ranking by order count or revenue, a Top-10 couriers ranking by orders completed with distance travelle… | manager / owner |
| ◐ | Live dashboard for the operator room (Лайвтайм Дашборд для операторской) | A real-time wall-board for the shift supervisor: В процессе (count of orders currently active — awaiting operator confirmation, cooking, or with a courier in transit) and Отменено (cancellations in the period,… | shift supervisor / call-cen… |
| ◐ | Bulk courier assignment (Массовое назначение курьера на заказы) | On the Orders page, select multiple orders via checkboxes, press Назначить курьера, pick one courier, and all selected orders are attached to that courier in a single action. Added because per-order manual ass… | operator / dispatcher |
| ◐ | Courier live tracking map (Показать на карте / trackcourier) | Shows in-house couriers who are using the courier app moving on a map, refreshed every 10 seconds. For each courier the operator sees the courier's name, their current order count, and their phone's battery ch… | operator / dispatcher / man… |
| ◐ | Courier order history (История заказов) | Lets a courier review their completed and cancelled orders. Each entry records completion time, payment status and delivery status (delivered or not delivered). | courier |
| ◐ | Bonus-payment visibility for couriers | When a customer paid part or all of an order using accumulated loyalty bonus points, the courier sees the exact bonus-paid amount in the app so they know how much cash to collect at handover. | courier |
| ◐ | Order handoff code verification (Код выдачи заказа / код курьера) | A verification step at branch pickup for aggregator orders: staff searches by Yandex order ID, the system loads the order (Yandex Eats and Delever identifiers, payment type and amount, customer info), the cour… | kitchen / branch staff; cou… |
| ◐ | Operator attribution on orders (кто принял / кто создал заказ) | The Orders page shows both who accepted the order and who created it, so managers can trace which operator handled which order and hold staff accountable. | manager / operator |
| ◐ | Operator action audit trail (История изменений) | Records which operator performed sensitive order actions rather than logging them as anonymous system events. Specifically fixed for two cases: sending an order to print now records the initiating operator's n… | manager / auditor |
| ◐ | Operator ↔ iiko operator mapping (Связка операторов между Delever и IIKO) | Lets an admin bind a Delever operator to an iiko operator by entering the iiko operator ID against the Delever operator in Настройки → Интеграции → IIKO → Операторы. When an order flows to iiko, the mapped ope… | admin configures; manager a… |
| ◐ | Courier transaction ledger with branch filter (транзакции курьеров) | A view of courier financial transactions in the admin panel, filterable by branch. | manager / finance |
| ◐ | Users and roles (Роль и доступ / Пользователи и роли) | Separate RBAC administration for admin-panel users, distinct from the Personnel section's operator and courier records. | admin |
| ◐ | Courier salary models guidance (модели зарплаты курьера) | Delever publishes a guide describing the three compensation models the platform is built to support: fixed salary (stable, loyal couriers, no speed incentive, suited to under ~30 orders/day); per-delivery paym… | owner / manager choosing a… |
| ○ | Operator product / upsell report (Отчёты по персоналу → По операторам → По пр… | Shows how much of each specific dish each operator sold, with three metrics per operator per product: Общее количество (portions added to orders), Глубина чека (receipt depth — average portions of that product… | manager / call-centre super… |
| ○ | Aggregator shift open/close notifications (Уведомления о сменах в агрегаторах) | Sends notifications when a shift opens and closes on connected aggregators. Originally these were always sent in Uzbekistan time; the system now sends them in the timezone of the company's registered country (… | manager / operator |
| ○ | Careers — vacancies and candidates (Контент → Карьера) | Recruiting front-door for staff. The Вакансии tab holds open and archived positions, each created with per-language name and conditions in Russian, English and Uzbek (with an Автоматический перевод button), a… | HR / manager |

**Open questions this section raises**

- Attendance (Посещаемость) is documented as a single screenshot with no prose. Is it a planned roster that managers author, an actual clock-in/clock-out log the courier app produces, or both reconciled against each other? The 'часы' column in the salary report is paid money, so Qoida must decide whe…
- How does a courier shift actually start and end? The settings toggle says orders are blocked unless the shift is 'activated according to the work schedule', but nothing documents whether the courier activates it themselves in the app, a manager activates it, or it opens automatically when the sched…
- Courier Fare carries a 'Тип тарифа: бонус или штраф' field, yet Bonuses/Penalties is a separate page with its own condition logic. Are these the same mechanism surfaced twice, or is the fare-level type a different concept? Qoida needs one coherent model rather than replicating Delever's ambiguity.
- What is the condition language for bonuses and penalties? The page is video-only. Are conditions evaluated automatically (on-time count thresholds, order counts, holiday calendar, weather) or entered manually by a manager per incident? Named drivers include damaged thermal bags and customer complai…
- What do 'Начальная минута' and 'Режим работы' on Courier Type actually control? Best reading is offer-timing and a working-mode/schedule mode, but neither is defined anywhere in the documentation.
- How does 'Вовремя' (on-time) get determined — against a promised delivery time on the order, against a fixed company-wide SLA, or against the report's 30/35/40-minute buckets? This directly determines payout, so the definition must be exact and immutable per order.
- Is the courier balance a liability the company owes the courier (accrued wages) or a prepaid float the courier tops up and the platform debits commissions from? The billing-mode description says fees are 'списание с личного баланса курьера', implying the latter, but the salary report's 'К оплате' i…
- Who settles courier payouts, and how? 'К оплате' is exported to Excel for accounting, suggesting Delever computes but does not disburse. Does Qoida stop at computing the figure, or does ADR 0013 need to cover outbound worker payments?

### Каталог (Catalog)

Delever's Catalog is a two-layer model: a tenant-wide **product catalog** (типизированные товары, категории, справочники) and a per-branch **menu (Меню)** that selects catalog items, sets per-channel prices/visibility/schedules/stock, and is what actually gets published to the storefront, POS and aggregators. Products are strongly typed into five kinds — Главный (parent), Вариативный (child variant), Простой (standalone), Модификатор (option), Комбо (bundle) — with modifier groups carrying min/max selection rules, and combo groups carrying choice-sets. Around this sit reference data (атрибуты, бренды, ингредиенты, теги, отделы, комментарии к продукту), merchandising (рекомендованные товары, автодобавление), pricing (прейскурант + per-channel price overrides), and analytics (ABC-XYZ, прогнозирование with holidays and a kitchen buffer). Note: the Catalog docs are overwhelmingly screen-rec…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Menu (Меню) | A named, per-branch assortment object that sits between the tenant catalog and every sales channel. You create a menu, add catalog products into it, and bind it to one or more branches ("Привязка меню в филиал… | manager, admin |
| ● | Bind menu to branch (Привязка меню в филиал) | Attaches a menu to a specific branch so that branch's storefront, QR, kiosk, POS and aggregator feeds all resolve from it. | manager, admin |
| ● | Per-order-type visibility inside the menu (Доставка / Самовывоз / Зал) | Each menu item can be switched on or off independently for delivery, pickup and dine-in, using the «Базовая цена» and «Зал» toggle columns. The documented use case: draught drinks sellable only in the hall, li… | manager |
| ● | Per-channel price overrides on a menu item | One product can carry different prices per channel simultaneously: Зал, generic Агрегаторы, and individually Yandex Eats, Wolt, Glovo, Uzum Tezkor, Bolt Food and others. Managed both from the menu screen and f… | manager |
| ● | Menu stock / remaining quantity (Остатки, логика остатков) | When «Использовать логику остатков» is enabled for the company, each menu item gets a quantity field; when it runs out the item is automatically hidden or blocked from ordering across all channels. A company-l… | manager, kitchen |
| ● | Product type: Main (Главный) | An abstract parent nomenclature card that is not itself a sellable SKU with a single price; it holds shared identity (name, article/артикул, description) and owns a «Вариации» block. After saving a Главный, th… | manager |
| ● | Product type: Variable / variant (Вариативный) | A concrete child variant of a Главный (size, flavour, execution). Carries its own price, weight, photo and description, and its own modifier set. Explicitly stated: a Вариативный товар does not exist in the ca… | manager |
| ● | Product type: Simple (Простой) | A standalone sellable item that needs no option selection at order time: name, article, description, price, unit of measure. Docs explicitly say that if you later need sizes or flavours you should switch to th… | manager |
| ● | Product type: Modifier (Модификатор) | An option/add-on/characteristic modelled as its own product record — priced if paid — with selection rules (required vs optional, group vs single). It is then linked to Простой products (and to variants). Edit… | manager |
| ● | Product type: Combo (Комбо) | A composite item assembled from products that already exist in the catalog. Carries its own name, article, description and either a fixed total price or dynamic pricing. Its structure is combo groups (МenuComb… | manager |
| ● | Category (Категория) | Hierarchical grouping of products. On create you set the name, choose root vs a parent category, set an active/visible status and a sort order. A category is only rendered on the storefront if it is active. Ca… | manager |
| ● | Brands (Бренды) | A registry of brands / trade marks ("Реестр брендов и торговых марок") that products belong to. Brand is also an operational scope above branch: stop-list changes sync across all brands and branch menus that s… | admin, manager |
| ● | Catalog base settings (Настройки → Каталог → Базовые настройки) | Three company-wide catalog switches: (1) «Использовать логику остатков» — turns on quantity tracking so items that run out are automatically hidden/blocked across every channel; (2) «Для QR и киоска: брать цен… | admin |
| ● | Stop-list (Стоп-лист) | Lets kitchen staff mark dishes temporarily unsellable so no new orders can be accepted for them, with the change propagating automatically to every connected sales channel. Operated from KDS on both desktop an… | kitchen, operator |
| ● | Catalog import from POS / 1C with external id mapping | A branch-scoped integration key authenticates a menu import that creates or updates categories, products and combos and, if the branch has a menu_id, appends new products to that branch's menu. Separate endpoi… | admin, integrator |
| ● | Fiscal and regulatory product fields — ИКПУ/MXIK, package code, marking | Every product, variant, modifier and modifier group can carry `ikpu` (MXIK) and `package_code`. The integrator API models these as `serviceCodesUz: {mxikCodeUz, packageCodeUz}` and states plainly "Специальные… | admin, manager |
| ● | Multilingual catalog content | Names and descriptions are localized objects, not strings. The integrator contract exposes `{ru, en, uz}` for category names, item names and `descriptionV2`; the platform has since added Azerbaijani (az) and r… | manager |
| ◐ | Copy menu into another menu / branch (Копировать в меню) | Bulk-clones the item set of an existing menu into another menu, explicitly aimed at chains rolling one assortment out across branches. Release note 23.06.2025 calls out "упрощено копирование меню — удобно для… | manager, admin |
| ◐ | Add products to menu with category filter and filtered select-all | When adding catalog items to a menu, the operator filters the catalog by category (and other filters) and only the filtered set is offered/added. "Выбрать все" respects the active filter and reports the true s… | operator, manager |
| ◐ | Mass 'enable for aggregator' (Включить для агрегатора) | One button opens an aggregator picker and then activates every item in the menu for the selected aggregators at base price, instead of toggling each item by hand when a new aggregator is onboarded. | manager |
| ◐ | Per-item sale schedule in menu (График продукта) | Menu items can carry a time schedule (weekday + from/till windows) restricting when they are sellable — e.g. breakfast items. The storefront pre-checks the schedule before checkout so the customer is told the… | manager |
| ◐ | Menu export to Excel / menu creation from Excel | Каталог → Меню exports the menu with item parameters to Excel; conversely menus, products and categories can be created by importing an Excel file. The import template supports a «Ссылка на фото» column with a… | manager, admin |
| ◐ | Aggregator menu preview (Предпросмотр меню) | Renders the menu exactly as it will appear inside Glovo, Wolt, Yandex Eats and Bolt Food, in both Mobile preview and Desktop preview modes, showing categories, item names, descriptions, prices and images, and… | manager |
| ◐ | Pre-publication menu validation (Предварительная проверка меню) | Before pushing a menu to Yandex Eats or Uzum Tezkor, the system checks that every field the aggregator requires is populated and emits a downloadable text report listing every deficiency. The cited required fi… | manager |
| ◐ | Hidden modifiers auto-selected by order type (Скрытые модификаторы) | A modifier can be set to «Видимость» = «Скрытый» with a variant «Количество по умолчанию» = 1, and the menu declares which order types (Доставка/Самовывоз/Зал) that variant applies to. The system then silently… | manager |
| ◐ | Nested variant-modifiers (Вариант-модификаторы внутри модификатора) | A modifier itself can contain variant options that are edited inside the modifier card (a release note fixes their save/rollback). So the option tree has depth: product → modifier group → modifier → modifier v… | manager |
| ◐ | Recommended products / cross-sell (Рекомендованные товары) | Inside a product card, a «Рекомендованные товары» block lets you attach related items (sauces with hot dishes, sides, accessories) with a display order, which then render as recommendations on the storefront a… | manager |
| ◐ | Product attributes (Атрибуты товаров) | A tenant-level registry of product characteristics — the V2 Settings description gives the examples размеры, вес, объем — that are then assigned to product cards. Effectively the dimension vocabulary that vari… | manager |
| ◐ | Ingredients (Ингредиенты) | A localized reference list of ingredients attached to products and surfaced to the customer at ordering time — the 23.06.2025 note groups it with tags as "сопутствующие элементы, которые отображаются при оформ… | manager |
| ◐ | Tags (Теги) | Small visual badges attached to products that render as an icon overlay on the storefront product card (screenshot shows a chilli icon pinned to the top-left of a burger card) and that feed the storefront «Фил… | manager |
| ◐ | Departments / kitchen stations (Отдел, Отделы) | "Категоризация и распределение блюд по цехам/отделам кухни" — every product is assigned to a kitchen department (hot line, cold line, bar, grill), which routes tickets and segments load. Forecasting is explici… | manager, kitchen |
| ◐ | Product comments (Комментарии к продукту) | A configurable set of canned comments / wishes for dishes ("Настройка быстрых комментариев и пожеланий к блюдам для клиентов/операторов") that the customer or the call-centre operator picks when adding an item… | operator, manager, kitchen |
| ◐ | Price list / bulk price management (Прейскурант) | A dedicated Catalog screen (slug `price-changer`) for managing product prices across the assortment. Documentation is a single screen recording with zero prose, so the exact mechanics are unverified; the surro… | manager |
| ◐ | ABC-XYZ analysis (ABC-XYZ) | Classifies every product on two axes over a chosen month range. ABC segments by revenue contribution: A ≈ the top ~80% of revenue (most critical items), B ≈ the middle ~15%, C ≈ the bottom ~5% (long tail kept… | manager |
| ◐ | Demand forecasting (Прогнозирование) | Predicts expected order quantity and revenue per dish so the restaurant can plan purchasing, staffing and kitchen load. The screen takes a date range plus a start-time and end-time defining the operating windo… | manager, kitchen |
| ◐ | Forecast holidays calendar (Праздники) | A dialog on the forecasting screen where the operator registers named holidays with a date range (Название + date range, saved to a paginated list). These are fed to the forecast so that atypical demand days a… | manager |
| ◐ | Forecast buffer configuration (Настройка буфера) | A configuration entry point from the forecasting screen (there is also a Кухня → Буфер page, which is empty in the docs). Paired with the documented cook-planning feature: the system computes how many cooks ar… | manager, kitchen |
| ◐ | Auto-add products, type 1 — Plain (Автодобавление, Обычный) | Unconditionally adds a configured product to the cart whenever the order matches the rule's context. Configured with: auto-add type = Обычный, applicable order types (Типы заказов), applicable sources/channels… | manager |
| ◐ | Auto-add products, type 2 — Product-triggered (Автодобавление, Товары) | Fires only when the cart already contains specified trigger products at or above a threshold quantity. Adds a configured product with its own min/max quantity and removable flag. Example given: if the order co… | manager |
| ◐ | Auto-add products, type 3 — Portion-band (Автодобавление, Порция) | Sums the total number of PORTIONS across the whole order (not line count, not money) and matches that total against operator-defined bands, each band naming a product and a min/max quantity to add. Documented… | manager |
| ◐ | POS import behaviours the catalog must tolerate | Documented import semantics: item display order is carried over from iiko so manual re-sorting is unnecessary; the import language is selectable per integration (Russian, English, O'zbekcha, Kazakh, Georgian…)… | admin |
| ◐ | Product physical and nutritional attributes | Product and variant cards carry weight, measure/measure_unit, energy/calories, proteins, fats, carbohydrates (КБЖУ) with an `is_deactivated` flag for invalid nutrition data, a `splittable` flag, `isCatchweight… | manager |
| ◐ | Product images and per-aggregator image selection | Products carry an ordered image list. The first image is the default sent to all aggregators; adding a second image and assigning it to a specific aggregator (e.g. Wolt) causes that aggregator to receive only… | manager |
| ◐ | Bulk product editing (Массовое редактирование товаров) | Select many products and either apply one shared ИКПУ / код упаковки to all of them, or edit Russian name, ИКПУ and package code for each of them in a single inline table. | manager, admin |
| ○ | Recipes (Рецепты) | Editorial recipe content — the client-app guide lists "Узнать интересные рецепты" as a customer feature alongside news, gallery and vacancies, and the 14.07.2025 release migrates Рецепты to V2 under the Контен… | manager (content) |
| ○ | AI-assisted product card completion | Per-field generation icons («ИИ генерация») on Вес, КБЖУ, ИКПУ and Код упаковки let the operator generate one field at a time during menu import, and results are editable afterwards. Separately, a «Сгенерирова… | manager |
| ○ | Catalog list ergonomics | Editing a product returns the operator to the same scroll position and preserves the selected category/type filters instead of resetting to page 1; product search handles fast typing without dropping character… | operator, manager |
| ○ | Direct product share links (multisite) | Each product gets a unique public URL and a «Поделиться» button in the storefront product card, hooking into the native OS share sheet on Android/iOS/desktop for Telegram, WhatsApp, Instagram, SMS, email. Prom… | manager |
| ○ | Marketplace catalog page (Multisite, тип сайта Marketplace) | A distinct storefront catalog experience for brands configured as Marketplace type, with a redesigned category navigation, a quick 'all categories' overlay for jumping between sections, and a more compact mobi… | manager |

**Open questions this section raises**

- Прейскурант (price-changer) is documented only as a screen recording. Is it (a) a bulk mass-price-update tool applying percentage/absolute changes across a filtered selection, or (b) a named price-list entity that menus/channels point at? The two imply very different schemas. Qoida must decide whic…
- How many named price planes exist? The evidence shows at least «Базовая цена» (delivery/pickup) and «Зал» (dine-in), plus per-aggregator overrides, plus a QR/kiosk redirect to the hall plane. Is 'channel price' an open-ended map keyed by channel id, or a fixed enum? Qoida should choose the open map.
- Availability and price are conflated in Delever's menu (price 0 once deleted the menu row; «Базовая цена» toggles delivery visibility). Should Qoida separate is_offered_on_channel from price_on_channel, and if so how do we express Delever-parity behaviour for migrating tenants (ADR 0024)?
- Are Атрибуты genuinely the variant axis (backing `size_id`), or a separate free-form spec-sheet vocabulary that happens to coexist with size_id? The docs describe them as 'размеры, вес, объем' which reads like a variant axis, but this is inferred, not stated.
- Does Qoida want ingredient-level BOM / costing / depletion at all? Delever does NOT have it — its Ингредиенты is a customer-facing localized composition label and its Рецепты is marketing content. If the product owner's parity brief assumed tech-cards, that assumption should be corrected before it…
- Portions (Порция) are a distinct decimal quantity dimension on the product, separate from line quantity, weight and unit of measure, and fractional values (0.5, 1.5) are supported. Does Qoida add a portions attribute to the product model, and does the cart sum it? Auto-add type 3 is unimplementable…
- Counted stock with an automatic daily reset (company default daily quantity) is beyond ADR 0017's shipped BINARY availability. Does Qoida extend the inventory ledger to counted per-menu-item availability with a scheduled daily seed, or model it as a separate lightweight per-menu counter?
- The forecast operating window defaults to 09:00→09:00 — the business day crosses midnight. Does Qoida adopt a tenant-configurable business-day boundary across reporting, forecasting and audit partitioning, or assume calendar days?

### Маркетинг (Marketing)

Delever's Marketing area is two things bolted together: a rule-driven discount/promotion engine that mutates order pricing, and a content-and-messaging suite (banners, stories, pop-ups, news, gallery, job ads, Telegram/SMS/push campaigns) that feeds every customer-facing sales channel from one place. A third pillar is "Тригеры" — event-driven transactional messaging (order status, birthday, cashback balance change, late order, OTP, funnel abandonment) with per-channel, per-source, per-order-type templates and dynamic variables. Referral link generation, customer tracking, and a kanban-style review/service-recovery board round it out. Note: Delever ships two admin panel generations; the older one (`/admin-panel/marketing`) is largely video-only, while the newer one (`/admin-panel-1`) splits Marketing (rules + messaging) from Контент (creative assets) — this inventory merges both.

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Marketing activity types: discount / markup / promo code (Скидка / Надбавка /… | A single 'marketing activity' record has one of three base formats chosen by radio button: a discount that lowers price, a markup (Надбавка) that RAISES price, or a promo code that only activates when the cust… | manager, marketer |
| ● | Discount application object: order vs product (Объект применения — Заказ / Пр… | Each rule declares whether it computes off the whole cart total (Заказ) or against specific line items (Продукт). Choosing 'Продукт' unlocks extra targeting panels for categories (Категории) and individual pro… | manager |
| ● | Fixed-amount discount (Фиксированный тип) | Subtracts an exact currency amount from the order total. The input field is denominated in the tenant currency (UZS shown in the docs). | manager |
| ● | Percentage discount (Процентный тип) | Reduces cost by a percentage; the field accepts 1–100 and renders a '%' suffix. | manager |
| ● | Free / discounted delivery rule (Доставка) | Grants free shipping once the order reaches a configured minimum price. The field is literally labelled 'Скидка будет действовать при цене от' (discount applies from price X). | manager |
| ● | Free-form discount for aggregators (Свободная скидка) | A technical rule type with NO input fields. It exists so third-party aggregators (Yandex Eats, Wolt, Uzum Tezkor, Express24, Chocofood, Foody) can compute a discount on their own side and push an arbitrary dis… | admin, integrator |
| ● | Product as gift (Товар в подарок) — order-sum variant | Automatically injects a free product into the cart when the order total crosses a threshold. Admin picks the trigger sum, the gift product, and how many units are added. | manager |
| ● | Product as gift (Товар в подарок) — cross-sell / 1+1 variant | Buy product A (in a specified quantity) and get product B free. The UI is two paired blocks: left = triggering product + required quantity, right = gift product + gift quantity. | manager |
| ● | Activation mode: manual vs auto-apply (Можно выбрать вручную / Автодобавление) | A toggle deciding whether a rule fires automatically on every qualifying order or whether an operator/cashier must deliberately attach it to a specific order. | operator, cashier, manager |
| ● | Eligibility by order type, source channel, and payment method (Тип заказа / И… | Each rule can be restricted to a set of order types (Доставка / Самовывоз / Зал / Агрегатор), a multi-select of originating sales channels (iOS, Android, Telegram bot, website, admin panel, kiosk, aggregator,… | manager |
| ● | Priority ordering across overlapping rules (Приоритет) | A numeric priority field; when several rules qualify for the same order, higher values are evaluated first. Combined with the non-stackable flag, the highest-priority rule is the one that wins. | manager |
| ● | Stacking and compatibility flags (Можно использовать с другими скидками / Мож… | Two independent booleans define combination behaviour. 'Combine with other discounts' ON = every qualifying rule applies simultaneously; OFF = only the single highest-priority rule applies. 'Works together wit… | manager |
| ● | Date range, daily time window, and 24/7 override (Дата / От времени / До врем… | A calendar start/end range bounds the campaign, and an intra-day from-time/to-time window enables happy-hour style promos. A '24/7' checkbox removes the time-of-day restriction while keeping the date range. | manager |
| ● | Audience and catalog scoping (Филиалы / Клиенты / Исключительные продукты / К… | Right-hand panel narrows the rule: to specific branches (or All), to named individual customers found by name/phone (leave blank = everyone, enabling one-off personal discounts), to a category set, to a produc… | manager |
| ● | SMS campaigns (СМС Рассылка) | Three tabs: Clients (marketing/service blasts), Couriers (operational SMS to delivery staff), and History (archive of past campaigns with per-recipient delivery statuses and statistics). The operator filters t… | marketer, manager |
| ● | Mobile app push notifications (Уведомление для приложений) | Composes a push with a cover banner (max 4 MB, 3:1 widescreen), localized title and description, and a 'receiver type' (Тип приемника) selecting platform/device group (iOS/Android) or user category, plus a sch… | marketer |
| ● | Telegram post broadcast (Telegram пост) | Blasts a rich post to the bot's user base: media attachment (max 1 MB, 1:1) typed as Photo / Video / GIF, body text with a 'Client Name' (Имя клиента) personalization token substituted at send time, a checkbox… | marketer |
| ● | Trigger: order status notifications (Тригеры — Статусы заказов) | Fires a templated message on order lifecycle transitions: 'Branch accepted' (Филиал принял), 'Cooking' (Готовится), 'Courier in transit' (Курьер в пути), and 'Completed' (Завершен). Each trigger is configured… | manager, admin |
| ● | Order status message variables (Ключевые слова) | Templates interpolate drag-and-drop tokens: order ID, restaurant name, customer name, customer phone, product list, total amount, discount amount, delivery fee, preparation time, delivery time, courier first n… | manager |
| ● | Trigger: late order apology with auto-generated promo code (Тригеры — Опоздав… | When delivery exceeds the established time limit, automatically sends an apology message that includes compensation. Critically, inserting the 'Промокод' (promo code) keyword into the template causes the SYSTE… | manager |
| ● | Trigger: SMS confirmation / OTP (Тригеры — СМС-подтверждение) | Defines the service SMS text used for user authorization and action confirmation. Localized ru/uz/en, with 'Код' (inserts the one-time code) and 'Источник' (inserts the platform name) variables. Configured per… | admin |
| ● | Review management kanban with service recovery (Управление отзывами) | A four-column kanban board for post-order feedback. Reviews land automatically in NEW (Новый) after order completion, showing date/time, star rating, customer name, order number, branch, and courier name. 'Beg… | manager, quality/ops |
| ● | Trilingual content with auto-translation (Языковые версии: Русский / Узбекски… | Every marketing content entity — promotions, banners, stories, pop-ups, news, gallery, vacancies, review tags, and all trigger templates — is authored in three parallel language tabs with an auto-translate but… | marketer, manager |
| ● | RFM customer segmentation as campaign targeting (RFM: Давность / Частота / Су… | Push and Telegram campaign registries expose RFM filters — days since last purchase (recency), order quantity (frequency), and average order value plus total spend (monetary) — and the SMS module offers the sa… | marketer |
| ◐ | Gift multiplicity mode (Условие добавления: Равно / Не менее) | Controls whether a gift rule repeats. 'Равно' (equals) makes the rule multiplicative — doubling the qualifying sum or quantity adds a second gift unit, tripling adds a third. 'Не менее' (at least) grants the g… | manager |
| ◐ | Nth-order targeting and first-order source (Заказы клиента / Источник первого… | Restricts a rule to a customer's specific ordinal order — e.g. only the 1st order (acquisition), or only the 5th (retention). Entering 0 removes the restriction. A companion dropdown pins which channel the cus… | manager |
| ◐ | Birthday-only restriction (Только на день рождения) | A checkbox restricting a rule to fire exclusively on the customer's birthday, as recorded on their profile. | manager |
| ◐ | Day-of-week schedule (Расписание по дням недели) | Seven independent toggles enabling or disabling the rule per weekday, supporting weekday-only or weekend-only campaigns. | manager |
| ◐ | Geo-zone restriction on discounts (Геозона) | A discount can be confined to hand-drawn polygons on an interactive map. Only orders whose delivery address falls inside a drawn boundary receive the discount. | manager |
| ◐ | Promotions content cards (Акции) | Separate from the pricing engine: an editorial promo card with cover image, localized title and description of terms, a call-to-action button label, a link, and optionally a bound product so tapping the card d… | manager, marketer |
| ◐ | Banners (Баннеры) | Slider creatives for the storefronts. Each banner has localized title text, an image OR video asset (media type is an explicit field), a click-through link, a numeric priority controlling rotation order in the… | marketer |
| ◐ | Stories (Истории) | Instagram-style story rails shown as circular icons at the top of the app, website, and Telegram bot. A story is a GROUP: one circular avatar icon, a title, a display date range, and an ordered list of slides.… | marketer |
| ◐ | Pop-ups (Поп-ап) | Modal overlay shown on storefront entry. Configurable with banner image, internal name, localized description, CTA button text and link, a date range, an intra-day time window (from/to), a 'show after' delay i… | marketer |
| ◐ | Trigger: birthday greeting (Тригеры — Дни рождения) | Automatically sends a congratulatory message on the customer's birthday as recorded on their profile. Single localized template (ru/uz/en) with client name and restaurant name variables, a default-language fal… | manager |
| ◐ | Trigger: cashback / bonus balance change (Тригеры — Накопительные) | Sends a message whenever a customer's bonus balance changes. Configured with a notification channel (Telegram, SMS, etc.), a Type selecting the operation category (accrual vs debit — accrual and write-off get… | manager |
| ◐ | Trigger: customer activity / funnel abandonment (Тригеры — Активность клиенто… | Watches for a user entering the Telegram bot or Mini App and then failing to complete an expected action, and sends an automatic nudge after a configurable delay. Fields: notification title (e.g. '🎁 A gift for… | marketer |
| ◐ | Referral link generation / customer tracking (Отслеживание клиентов) | Creates trackable acquisition links for campaigns and influencers. Two types: Website (Веб-сайт) auto-builds https://[domain]/?ref=[label] from an admin-supplied label, and Bot (Бот) builds a Telegram deep lin… | marketer |
| ◐ | Telegram Mini App / referral setup (Рефералы) | Documents the BotFather-side configuration that makes referrals work: creating or selecting a bot and obtaining an API token, setting the Menu Button label and URL, configuring the Mini App with a full-screen… | admin, integrator |
| ◐ | Referral program (Реферальная программа) | A distinct menu item from referral links, sitting alongside them under Marketing. Delever's documentation page for it contains no descriptive text (video-only), so the actual reward mechanics — who is rewarded… | marketer, manager |
| ◐ | Review tag library (Отзывы — быстрые ответы) | Configurable one-tap feedback chips shown to customers instead of free-text, each with localized text and an emoji/icon (max 1 MB, 3:2). Every tag is classified by what it refers to (Относится к: operator/call… | manager |
| ◐ | SMS template library (Шаблоны СМС) | The v1 equivalent of the trigger templates: per-language (Uzbek/Kazakh, Russian, English) SMS bodies with drag-and-drop variables (order ID, restaurant name, delivery time, product list, total amount, discount… | admin |
| ◐ | Promo code performance report (Отчёты по маркетингу — по промокодам) | Three tabs. General (Общий отчет) lists each promo code with the number of orders it was successfully applied to and the aggregate discount/receipt value. By-promo-code drills into one code showing customer, s… | manager, marketer |
| ○ | News (Новости) | Editorial posts (e.g. 'Opening of a new branch in Chilanzar') with a cover image, localized headline and long-form body, and a publication date range after which the post auto-deactivates. Surfaced on the webs… | marketer, manager |
| ○ | Photo gallery (Галерея) | Albums of venue/dish photography with a localized album title and description, multi-upload of images (max 1 MB each, 3:2 recommended), individual image removal, and a display date range. Intended to drive foo… | marketer |
| ○ | Job postings and candidate pipeline (Вакансии / Карьера + Кандидаты) | Two tabs. 'Vacancies' holds open and archived positions with a localized title and a free-text description carrying requirements, working conditions, schedule and pay (no separate salary or branch fields — all… | manager, HR |

**Open questions this section raises**

- Referral program rewards are entirely undocumented (video-only page): who earns the reward (referrer, referee, or both), whether it is cashback/promo code/discount, the qualifying event (registration vs first completed order), amounts, caps, and expiry. Qoida must specify this from scratch.
- Loyalty/cashback configuration is undocumented, yet cashback is referenced by the discount compatibility flag, the Накопительные trigger, and review compensation. Accrual rate, per-branch or per-channel rates, redemption caps as a share of order value, point expiry, and whether points accrue on dis…
- Stacking arithmetic is unspecified: when two or more stackable percentage discounts qualify, are they applied sequentially (compounding) or summed additively, and is the base the pre- or post-discount subtotal? Also undefined: tie-breaking when two rules share the same priority value.
- No per-customer or global usage limits on discounts and promo codes appear anywhere in the docs (no 'max N uses', 'once per customer', or total budget cap). Either Delever lacks them or they are undocumented — Qoida almost certainly needs them.
- Promo code uniqueness model is ambiguous. The late-order trigger clearly mints unique per-customer codes, but campaign promo codes look like shared code words. Whether these are one entity with a uniqueness flag or two distinct concepts needs deciding.
- Late-order detection thresholds are not documented: what counts as late (promised time vs SLA vs a configurable grace period), who owns the promise, and whether staff are notified alongside the customer.
- The 'Свободная скидка' aggregator type accepts an externally computed discount with no validation. Qoida must decide whether to accept unvalidated external price adjustments, and how such orders are reconciled, audited, and fiscally reported when the platform cannot re-derive the total.
- Consent and opt-out are absent from the marketing docs. There is no unsubscribe flag, no marketing-consent field, and no suppression list on SMS/push/Telegram campaigns — ADR 0015 covers consent, so the link between consent state and campaign recipient selection must be defined.

### Кухня (Kitchen)

The Кухня section is Delever's in-restaurant execution surface, marketed separately as "KDS" (Kitchen Display System) and licensed per branch per month (~390,000 UZS / $40, listed in the deposit-based module pricing as "🍳 Кухня — за филиал/месяц"). It gives branch staff — cooks, expo, cashiers, branch managers — a screen-first replacement for paper tickets: incoming kitchen orders split by fulfilment type, a buffer/holding queue, a VDU wall display, an in-branch product-sales screen, an expo/handover ("Раздача") station, a stop list for instant availability control, and a Yandex-specific courier handover-code check. It exists in desktop and mobile/tablet variants (KDS v1 and KDS v2) and has been progressively absorbing operator powers — assigning own couriers, dispatching third-party delivery services, changing payment type, and toggling the branch open/closed — so a branch can run serv…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Kitchen Display System module as a whole (Кухня / KDS) | A branch-scoped operational screen set, separate from the admin panel's operator/dispatcher surface, that puts live orders in front of kitchen and counter staff and lets them drive an order to completion witho… | kitchen staff, cashier, bra… |
| ● | Kitchen orders (Кухонные заказы) | The primary queue screen: orders routed to this branch, split into at least two fulfilment streams that the page documents as separate tabs — Pickup (Самовывоз) and Delivery (Доставка). Staff work the order th… | kitchen, expo |
| ● | VDU — visual display unit (VDU) | A separate wall/monitor display distinct from the interactive Кухонные заказы screen. In the R-Keeper/UCS convention that this market inherits, KDS is the interactive station screen for concept kitchens while… | kitchen line, counter |
| ● | Stop list (Стоп-лист) | THE ONLY FULLY SPECIFIED SCREEN. Lets kitchen staff take a product out of sale immediately and put it back, from KDS desktop (Кухня > Стоп-лист) or KDS mobile (nav bar > Кухня > Стоп-лист). Two tabs: «Доступны… | kitchen, branch manager |
| ● | Stop-list propagation to every sales channel | A stop set in KDS suppresses the item across the multisite, mobile app, Telegram bot and mini-app, kiosk, QR-menu, and every connected aggregator, and blocks it at checkout in the admin panel too. Documented e… | kitchen, operator, customer |
| ● | Order handover code check (Код выдачи заказа) | A dedicated screen for handing a prepared order to an aggregator courier. Staff search by Yandex order ID in a «Поиск по ID заказа» field; the system loads the order and shows its identifiers (Yandex Eats ID a… | expo, cashier |
| ● | Branch open/closed toggle from KDS (Управление статусом филиала в KDS) | Kitchen staff can switch the branch's availability on and off directly from the KDS interface, without going to another section or asking an operator. Used to stop taking orders during an overload or a tempora… | kitchen, branch manager |
| ● | Courier assignment and third-party dispatch from KDS | Branch staff assign the restaurant's own courier to an order from inside KDS (v1, 25.05.2026), and from mobile KDS v2 (01.06.2026) they can additionally call external delivery services directly — Noor, Milleni… | kitchen, branch manager |
| ● | Preorder kitchen fire time (время печати чека на кухне) | For preorders from any channel, an operator or manager can manually set the moment the ticket prints in the kitchen — i.e. when the kitchen actually starts the job — independently of the promised delivery time… | operator, manager, kitchen |
| ◐ | Buffer (Буфер) | NOT DOCUMENTED — the page exists in the navigation, in both V1 and V2 doc trees, and has an empty body. By its name and its position between Кухонные заказы and VDU, it is a holding area: orders that have been… | kitchen, branch manager |
| ◐ | Product sales (Продажи продуктов) | NOT DOCUMENTED IN PROSE — video-only page. Positioned between VDU and Раздача in the kitchen workflow, so it is the in-branch selling/counter screen: ringing up walk-in or over-the-counter sales of ready produ… | kitchen, cashier |
| ◐ | Distribution / expo pass (Раздача) | NOT DOCUMENTED — empty page in both doc trees. 'Раздача' is the expo/pass station: the point where a cooked order is assembled from the stations, checked against the ticket, packed, and handed to a courier or… | expo, branch manager |
| ◐ | Per-aggregator stock threshold auto-stop | Each aggregator integration (Glovo, Chocofood, Foody, Wolt, Yandex, Uzum) has its own configurable remaining-quantity limit. When a product's remaining count falls to that limit, Delever automatically sends a… | manager, admin |
| ◐ | Stop-list change notifications to Telegram | When menu items go on or come off stop, a bot posts a digest into the orders group listing «Позиции на стопе:» with 🛑 per item and «Вышли со стопа:» with ✅ per item, headed by the menu name. A dedicated Telegr… | kitchen, branch manager, op… |
| ◐ | Change payment type from KDS | Branch staff change an order's payment type directly in KDS, desktop (v1, 25.05.2026) and mobile (v2, 01.06.2026), rather than routing the change through an operator — for the common case where a customer chan… | kitchen, cashier |
| ◐ | Order creation from KDS with address suggestions | KDS carries its own order-creation flow, including address autocomplete/suggestions when typing a delivery address (repaired and rebuilt for Admin V2, 18.08.2025), fully adapted to phones and tablets in KDS v2… | kitchen, cashier |
| ◐ | Kitchen departments / stations (Отделы) | Products are categorised and distributed across kitchen departments/shops (цеха/отделы), configured under Настройки → Каталог → Отделы. This is the routing key that decides which station or screen a given line… | admin, kitchen |
| ◐ | Kitchen-relevant order handling settings | Settings elsewhere in the admin panel that directly govern what the kitchen sees and when: «Автопринятие заказа» with a per-channel source list and a minimum-successful-orders threshold before auto-accept appl… | admin, manager |
| ○ | Kitchen load forecasting and cook-count planning (Прогнозирование) | Forecasts demand and computes how many cooks are needed to produce the forecast volume in time. Per branch and per product, an administrator enters the maximum number of portions that can be cooked per hour; t… | manager, admin |
| ○ | Live branch load visibility | When creating an order, the branch picker shows the count of active orders next to each branch name, so whoever is placing the order can steer away from an overloaded kitchen. Order counts per status in the ad… | operator, manager |

**Open questions this section raises**

- Буфер (Buffer) has NO documentation at all — an empty page in both doc trees. What does it actually hold: preorders awaiting their fire time, unpaid orders awaiting payment confirmation, orders parked during a rush, or something else entirely? What releases an order from it — a timer, a manual acti…
- Раздача (Distribution/expo) also has NO documentation — empty page. Is it a per-department assembly checklist, a simple ready-for-handout queue, or a packing/verification station? Does it operate per kitchen department (Отдел) with independent ready states that roll up to an order-level ready, or o…
- Продажи продуктов (Product sales) is video-only. Is it a counter-sale/POS screen that originates orders from the kitchen, or a read-only per-product sold/remaining readout for the shift? The two readings imply very different work — one touches ADR 0002 order acceptance and pricing, the other is a r…
- What is the exact relationship between VDU and Кухонные заказы? Is VDU a passive read-only wall display of the same data, a differently-filtered view, or a separate device class with its own configuration? The R-Keeper convention suggests KDS = interactive station screen, VDU = fast-food display bo…
- Does a stop entry ever expire on its own? Nothing in the documentation mentions shift-end auto-clear, a duration, or a scheduled un-stop — it appears to persist until cleared manually or by the upstream POS. Qoida should decide deliberately whether stops are indefinite, shift-scoped, or time-boxed,…
- What exactly is the scope key for a stop? The docs show branch-level, POS-terminal-level (iiko), menu-level, brand-level and per-aggregator-channel variants, and a fix that propagates one stop across all brands and branch menus containing the same position. Qoida needs one coherent scope model rath…
- Are modifiers and modifier variants stoppable? The Poster integration explicitly excludes them (stops apply only to main products), but a separate 2025 fix concerned 'modifier display in the stop list'. Inconsistent — needs a decision for Qoida.
- Does the handover-code check (Код выдачи заказа) generalise beyond Yandex Eats? The screen is written entirely around Yandex order IDs and Yandex-supplied codes. Do Wolt, Uzum Tezkor, Express24, Chocofood or Foody use comparable handover protocols that Qoida would also need to support?

### Настройки (Settings)

Delever's Settings section is the tenant-level control plane for a restaurant brand: it owns the company profile and branch registry (hours, prep-time intervals, order limits, per-branch Telegram alert routing, geo point), the global order-processing rulebook (VAT and fiscalization, SLA timings, auto-accept, auto-dispatch, batching, zone restrictions), delivery and courier economics (delivery tariffs, courier types/fares, bonuses/penalties, GPS action validation), geozones (delivery zones, branch geozones, regions with bounding boxes), catalog-wide switches (stock logic, in-dine pricing for QR/kiosk), the payment-type and sales-channel registries, the reference data for cancellation/completion reasons and review tags, users/roles with permission-based sidebar gating, and a change-history audit log. Two things here are Delever's OWN SaaS commerce rather than the restaurant's: "Тарифы" (s…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Company main settings / brand profile (Основные настройки) | Single master profile for the whole tenant: brand logo, an extra media asset used specifically for aggregator listings and the QR menu, trade name, primary phone, official Telegram bot handle, description, and… | admin |
| ● | Branch registry and branch profile (Филиалы) | The list and full edit form for every physical point. Table shows cover image, name, delivery hours, active/inactive status and creation date, plus a bulk 'Load geozones' action. The edit form covers identity… | admin, manager |
| ● | Branch operating and delivery schedules (Рабочее время филиала и Часы доставк… | Per-day-of-week open/close times configured separately for the venue and for the delivery service, with a 'works' toggle per day and a 24/7 shortcut for round-the-clock points. Delivery hours are shown as a di… | admin, manager |
| ● | Order base settings — payment and fiscalization block (Настройки → Заказы → Б… | Tenant-wide tax and fiscal receipt configuration: VAT percentage applied to receipt lines, the INN used for fiscalization, an explicit list of which payment types Delever itself fiscalizes automatically, a pay… | admin |
| ● | Order timings, SLA and distance metrics (Опции) | Defines the global operating window and the SLA clock for every order: business day start/end, average order time, maximum order time, the minute threshold after which an order is flagged 'late', the highlight… | admin |
| ● | Order automation — auto-accept and auto-dispatch (Автоматизирование) | Auto-accept moves incoming orders straight to 'Accepted' with no operator, restricted to a chosen set of sales channels (e.g. Telegram bot, website only), and gated by a minimum count of prior successful order… | admin |
| ● | Payment types registry (Типы оплаты) | A tenant-managed catalogue of customer-visible payment methods. Each entry carries a multilingual display name (RU/EN/UZ), a 1:1 icon up to 1 MB, an active flag, and a system payment type that determines behav… | admin |
| ● | Sales channels registry (Каналы продаж, v1 'Типы заказов') | A tenant-managed list of order-origin channels. Each has a free-text display name and a system channel type from a fixed enum: iOS, Android, Kiosk, Bot, Website, Admin panel, Aggregator, Hall (dine-in), QR, ka… | admin |
| ● | Delivery tariffs (Тарифы доставки / v1 Тарифы) | Named delivery pricing schemes bound to branches and to delivery zones. Resolution has an explicit precedence: if a branch has no tariff but the delivery zone matching the customer address does, the ZONE tarif… | admin |
| ● | Delivery base settings and courier control (Настройки → Доставка → Базовые на… | Global courier operating rules: enable courier billing (deduct commissions/fees from the courier's personal balance per completed delivery); enforce the courier's rostered shift before they may accept orders;… | admin |
| ● | Delivery zones (Зоны доставки) | Map-drawn polygons defining where delivery is offered. Each zone carries its own delivery tariff (which outranks the branch tariff), an optional free-delivery-from threshold, and participates in branch/service… | admin |
| ● | Branch geozones (Геозона филиалов) | The catchment polygon owned by a branch, distinct from the shared delivery zones. Can be bulk-uploaded from the branch registry via a 'Load geozones' action; release notes cover fixing coordinate/boundary pers… | admin |
| ● | Users, roles and access (Пользователи и роли / Роль и доступ) | Defines staff roles and the fine-grained permissions attached to them. The v2 route map states that a sidebar section is rendered only when the user holds the associated 'get' permission, and that module locks… | admin |
| ● | Subscription tariffs and purchasable modules (Тарифы) | Delever's own SaaS commerce, exposed inside the customer's admin panel. After the free trial a one-off $500 deposit is required to activate services, credited to the account balance and then drawn down for sub… | admin |
| ● | Prepaid account balance (Баланс) | A prepaid wallet funding the subscription and modules. Top-up runs from Settings → Balance → Balance button: enter an amount, pay via Click, then either 'invoice' flow (enter the phone number linked to the Cli… | admin |
| ● | Markups, discounts and promo codes (Надбавки и скидки / Скидки) | The promotion engine. Each activity is named in RU/UZ/EN and typed as Discount, Markup (a surcharge, not a discount) or Promo code. It applies either to the Order (whole basket) or to a Product. Order-level of… | admin, manager |
| ● | Discount activation, targeting and stacking rules | Every promotion carries a large conditions envelope. Activation is either manual (an operator picks it per order) or automatic. Scope is constrained by order type (delivery, pickup, hall, aggregator), by sourc… | admin, manager |
| ● | Cancellation reasons (Причины отмены) | A managed reference list of order cancellation reasons. Each entry has an internal name for operators, a separate customer-facing wording (deliberately softer, shown in the app or bot when the order is cancell… | admin (config), operator (u… |
| ◐ | Branch preparation-time intervals (Интервал времени приготовления заказа) | Beyond a single base prep time, a branch can add multiple time-of-day intervals each carrying a different cooking duration, so peak hours quote a longer prep time than quiet hours. Combines with separate 'preo… | admin, manager |
| ◐ | Per-branch Telegram alert channels (Интеграция с Telegram — ID чатов) | Each branch stores up to five separate Telegram chat IDs so different classes of event fan out to different operational groups: routine order notifications, system errors, cancelled orders, courier auto-dispat… | admin |
| ◐ | Version management / feature flags (Управление версиями) | Per-tenant toggles that switch individual admin modules between v1 and v2 interfaces: Orders v2, other sections v2, Bot settings v2, Balance v2, Dashboard v2, Kitchen orders (KDS) v2. Documentation explicitly… | admin |
| ◐ | Order conditions — branch limits and zone containment (Условия) | Two guards. Branch order limiting enforces the per-branch max-concurrent-orders ceiling. 'Do not accept orders from other delivery zones' forbids the system from silently substituting a branch from a different… | admin |
| ◐ | Order acceptance ordering and paid-only visibility (Дополнительные условия) | Selects whether a courier must accept the order first or the branch/kitchen must confirm readiness first — reversing the orchestration sequence. A second toggle hides unpaid orders from both the courier app an… | admin |
| ◐ | Courier order batching (Группировка заказов для курьера) | Rules for combining several orders into one courier trip, with a 'priority order radius' in metres defining the maximum distance between drop-off points that still permits batching. Multiple rules can be added. | admin |
| ◐ | Preorder branch resolution outside working hours (Приоритет для нахождения фи… | Chooses the tie-break rule for which branch fulfils a preorder placed while everything is shut: 'by distance' picks the branch nearest the customer, 'by time' picks the branch that opens first. | admin |
| ◐ | Automatic product addition (Автодобавление / Продукт на заказ) | Rules that inject items into the basket automatically — cutlery, sauces, chopsticks. Three rule kinds. 'Обычный' adds a fixed product for chosen order types and sources with min/max quantity. 'Товары' triggers… | admin |
| ◐ | Loyalty program settings (Программа лояльности) | A settings tab for bonus accrual and discount rules; in the v2 route map it lives at /home/marketing/loyalty. The documentation page is a stub. Surrounding release notes show a bonus-points balance per custome… | admin |
| ◐ | Free-delivery threshold per zone (Бесплатная доставка от) | A per-delivery-zone toggle plus an amount: orders above that basket total get free delivery. A release note makes clear this now zeroes the delivery charge even when the cost came from an external courier serv… | admin |
| ◐ | Courier types (Тип курьера) | A vocabulary of courier vehicle classes — car, motorbike, bicycle, cargo — each carrying a minimum distance in metres it is eligible for, a starting-minute offset, and a work mode. Used to match couriers to or… | admin |
| ◐ | Courier fares (Тариф курьера) | Compensation schemes for internal couriers, composed of a fixed amount, a per-order amount and a per-kilometre amount, and typed as either bonus or penalty. Attendance data (courier, date, time) is viewable al… | admin, manager |
| ◐ | Courier bonuses and penalties (Бонусы/Штрафы курьеров) | Rule-based adjustments to a courier's balance: bonuses credit the courier's balance when defined conditions are met, penalties debit it when conditions are breached. | admin, manager |
| ◐ | GPS radius validation of courier actions (Проверка действий курьера по радиус… | Anti-fraud geofencing on courier status transitions. A master toggle enables GPS validation; one radius (in km) limits how far from the pickup point a courier may accept an order; a second radius (in metres) l… | admin |
| ◐ | Regions with search bounding box (Регионы) | Defines a city/region by a central address (map pin auto-moves to the geocoded address) plus an explicit rectangular bounding box given as SW latitude/longitude and NE latitude/longitude. The bounding box cons… | admin |
| ◐ | Catalog base settings — stock logic and channel pricing (Настройки → Каталог… | Three tenant-wide catalog switches. 'Use stock logic' turns on quantity tracking: when a product runs out it is automatically hidden from sale or made unorderable across every channel at once. 'For QR and kios… | admin |
| ◐ | Catalog attributes (Атрибуты) | Manages product characteristics used to build variable products — sizes, weight, volume and similar. | admin |
| ◐ | Brands registry (Бренды) | The register of brands and trade marks under the account. Screencast-only. | admin |
| ◐ | Kitchen departments / stations (Отделы) | Categorises dishes into kitchen departments/stations so preparation work is distributed to the right section. Screencast-only. | admin, kitchen |
| ◐ | Debt notification and feature restriction (dunning) | When an account is in arrears the system automatically raises an in-panel notification; while unpaid, access to some Delever functions is restricted. Once payment lands the notice disappears and access is rest… | admin |
| ◐ | Completion reasons (Причины завершения) | A managed reference list of terminal-success outcomes an operator picks when closing an order — 'Delivered', 'Order collected', 'Delivered by third-party service', 'Pickup completed'. Simple name-only records… | admin (config), operator (u… |
| ◐ | Review characteristic tags (Отзывы) | The vocabulary of one-tap feedback tags customers pick after rating an order, so they can give precise feedback without typing. Each tag has multilingual text (often with an emoji), an optional 3:2 icon up to… | admin (config), customer (u… |
| ◐ | Review management CRM board (Управление отзывами) | A Kanban board for service recovery. New reviews land automatically in 'New' carrying date/time, star rating, customer, order number, branch and courier. 'Start work' moves a card to 'In progress', where the h… | manager, support |
| ◐ | Change history / audit log (История изменений) | A settings-level audit journal of user actions and integration events, with filters by parameter and time period. A 'What changed?' column opens a before/after diff of the modified values. Release notes show p… | admin, support |
| ◐ | SMS templates (SMS шаблон) | Per-status customer messaging templates authored separately in Uzbek (or Kazakh), Russian and English. Merge variables are dragged and dropped into the body: order ID, restaurant name, delivery time, product l… | admin |
| ◐ | Aggregator partner registry (Агрегатор / Partners) | A Settings page (route /home/settings/aggregator, described in the v2 route map as 'Partners — configure aggregator partners, when enabled') where the tenant registers aggregator partners that will push orders… | admin |
| ○ | Branch tags (Теги филиалов) | A tag vocabulary for grouping and labelling locations; tags are assigned on the branch form and used for internal grouping. Documentation page exists but is empty. | admin |
| ○ | Review settings (Настройки отзывов) | A dedicated settings tab under Orders for parameters governing customer feedback collection. The documentation page is a stub with no content. | admin |
| ○ | Free geozone (Бесплатная геозона) | A separate geozone layer listed under Company settings in the v1 tree, alongside branch geozones and delivery zones. Screencast-only, no prose. | admin |
| ○ | Product comment presets (Комментарий к продуктам) | A vocabulary of quick preset comments/wishes customers or operators can attach to a dish instead of free-typing. Documentation page is a stub. | admin |

**Open questions this section raises**

- Тарифы (Settings, v1) is ambiguous in the docs: the v1 page is screencast-only and the term is used for BOTH Delever's own SaaS subscription plans (with purchasable modules and a deposit-funded balance) and for delivery tariffs (which v2 relocated to Settings → Доставка → Тарифы доставки). Qoida sh…
- Delever exposes three overlapping geometry layers — branch geozone, delivery zone, and 'free geozone' — and the docs never explain how they interact or which wins. Does Qoida need three, or can one zone entity with typed roles cover all cases?
- Delivery cost has at least four possible sources (branch tariff, zone tariff which outranks it, the live courier-service quote, and a promotion granting free delivery), plus a per-zone free-delivery threshold. The full precedence order is never stated in one place. Qoida must define it explicitly i…
- The promotion engine is priority-ranked, optionally stackable and conditionally compatible with cashback. Delever's own release notes record repeated double-deduction and cross-channel divergence bugs here. What resolution algorithm does Qoida commit to, and is a quote reproducible from a versioned…
- Loyalty (Программа лояльности) and review settings (Настройки отзывов) are documented only as empty stubs. Bonus points, expiry, and deposit accounts appear all over the release notes and payment-type enum but their rules are undocumented — this needs a direct product-owner conversation, not more d…
- Bonus points and customer deposit are separate balances, and both are selectable as payment types. Is Qoida taking on customer-held stored value, and if so under what regulatory framing in Uzbekistan?
- Delever's balance funds have an expiry date. Should Qoida prepaid credit expire? It changes the ledger design and requires a scheduled expiry-and-notify job.
- Access control stacks three orthogonal gates: user permission, purchased module (entitlement), and tenant 'business type' (a courier-service tenant gets a different default route and different sections). Qoida ADR 0025 and 0021 cover the first two; business type as a navigation/capability axis is n…

### Settings → Integrations (Настройки → Интеграции)

The Integrations section is Delever's provider-binding hub: it is where a tenant connects its restaurant to everything outside the platform, organised into six categories — sales channels (Каналы продаж), POS systems (POS-системы), order aggregators (Агрегаторы заказов), delivery services (Службы доставки), payment gateways (Платёжные шлюзы / Платёжные системы), analytics services (Сервисы аналитики) and SMS providers (SMS-провайдеры). The architectural spine is that almost every integration is installed per branch (филиал), not per tenant: credentials, service IDs, fiscal INNs and even API tokens are held at branch granularity, and a "sales channel" (источник) is a first-class configurable entity that gates which payment methods, order types, menu items and even prices a customer sees. Delivery-service integrations are not just courier APIs — each carries a rule engine ("автовызов курь…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Sales channels registry (Каналы продаж) | A registry of order sources, each a named row with a system type. Every order carries its source, and nearly every other integration setting is keyed by it. Types observed: iOS, Android, Kiosk (Киоск), Telegra… | admin / tenant owner |
| ● | Content settings — per-channel capability matrix (Настройки контента) | A grid where rows are sales channels (iOS, Kiosk, Bot, Website, Admin Panel, Android, QR) and columns are capabilities, ticked per cell. Four independent matrices: (1) available payment methods per channel — C… | admin |
| ● | Telegram bot channel (Настройки бота) | Connects a tenant-owned Telegram bot as a full sales channel. The admin pastes a BotFather token and clicks 'Verify' — Delever validates it and auto-populates the bot's display name and @username. Configuratio… | admin / marketing |
| ● | Website channel settings (Настройки сайта) | Provisions and configures a customer-facing website per tenant. Domain choice is either a delever.uz subdomain (name checked for availability in-panel) or the tenant's own domain, which must be routed through… | admin / marketing |
| ● | Self-service kiosk channel (Киоск самообслуживания) | Registers a physical self-service terminal as its own sales channel instance. Each kiosk record carries: internal name, legal company name, the branch it belongs to, and — importantly — a customer profile unde… | admin (setup) / kiosk devic… |
| ● | POS integration: iiko / Syrve | The most fully documented POS binding, with per-branch configuration tabs for Credentials (Полномочия), Branches, Products, Terminal, Couriers, Discounts, Aggregators and Cancellation reasons. Delever offers t… | admin (setup) / cashier (co… |
| ● | POS integration: R-Keeper via XML interface | Connects to R-Keeper's cash server over its XML interface. Requires one of three specific R-Keeper XML licences on the site. The dealer must supply Delever with: an HTTP user login/password created in the R-Ke… | admin / R-Keeper dealer / i… |
| ● | POS integration: R-Keeper WH (WhiteServer) | The cloud alternative to R-Keeper XML, and the one with the fullest UI flow. Step 1: create a configuration binding a Delever branch to an R-Keeper branch ID, an order-category code and a system name. Step 2:… | admin / integrator |
| ● | POS integration: 1C (with full HTTP API specification) | The only POS integration with a published API contract. All routes are prefixed /v1/1c. Authentication is a raw integration key in the Authorization header (not Bearer), and that key resolves to a specific Del… | integrator / 1C system |
| ● | Order aggregator: Uzum Tezkor (UzumTezkor) | Inbound order aggregator integration — Uzum Tezkor is the food-delivery arm of Uzum, Uzbekistan's largest marketplace/fintech group. Configured per branch, with a payment-type mapping tab pairing Uzum Tezkor p… | admin / kitchen |
| ● | Order aggregator: Yandex Eda (Яндекс Еда) | Inbound order aggregator integration with Yandex's food-delivery marketplace, configured per branch. Documented as screencast only. | admin / kitchen |
| ● | Dynamic (generic) aggregator integration (Динамическая интеграция) | A self-service escape hatch for connecting an aggregator Delever has not hard-coded. Credentials are minted inside the panel: the tenant creates a role of user type 'Aggregator' under Settings → Users and role… | admin / partner integrator |
| ● | Partner/integrator public API (Для интеграторов) | The documented outbound API that aggregators and partners call to integrate with a Delever tenant. OAuth token endpoint at /v1/custom-integration/security/oauth/token taking client_id and client_secret (base64… | partner integrator |
| ● | Delivery service: Yandex Delivery / Yandex Go (Яндекс доставка) | Outbound courier sourcing from Yandex Go, split into a Credentials tab (global) and a Branches tab (per-branch account binding). Authorisation: a single API token, or per-branch tokens via the 'use a separate… | admin (setup) / dispatcher… |
| ● | Delivery service: Uzum Delivery | Outbound courier sourcing from Uzum's logistics arm, with an interface essentially identical to Yandex Delivery: Credentials and Branches tabs; single or per-branch API token; order-cancellation timeout for un… | admin / dispatcher |
| ● | Payment gateway: Payme (+ Payme Go) | Acquiring integration with Payme, one of Uzbekistan's two dominant payment systems. A registry screen lists connected branches with their merchant IDs and creation dates, searchable by branch name or Merchant… | admin / finance |
| ● | Payment gateway: Click (+ Click PASS) | Acquiring integration with Click, Uzbekistan's other dominant payment system, configured per branch. Credentials: secret Key, Service ID, Merchant ID and Merchant User ID from the Click cabinet, plus an option… | admin / finance |
| ● | Payment gateway: Uzum (Uzum Bank) | Acquiring integration with Uzum Bank, structured as two tabs. 'Credentials' (Полномочия) holds a cancellation reason — a dropdown whose selected value is automatically transmitted to Uzum whenever an order is… | admin / finance |
| ● | Payment gateway: TipTop Pay (+ Google Pay and Apple Pay) | The card-on-file and wallet gateway. Per branch, TipTop supplies a Public ID and an API password. Critically, a default branch must be designated in the Credentials tab, because TipTop stores a saved card scop… | admin / finance |
| ● | SMS provider: Eskiz (Eskiz.uz) | Configures the outbound SMS gateway used for both service notifications and marketing messages to customers. Fields: provider selected from a dropdown (Eskiz), the login or email registered at Eskiz.uz, the ac… | admin |
| ● | Per-branch integration installation model (Филиалы tab, ubiquitous) | A structural pattern rather than a single screen: virtually every integration in the section — Payme, Click, Uzum, Kaspi, TipTop, iiko, R-Keeper, Jowi, Poster, Wolt Drive, Yandex Delivery, Uzum Delivery, Wolt,… | admin |
| ● | Cross-cutting mapping tables (products, payment types, discounts, couriers, c… | Every POS and aggregator integration maintains bidirectional mapping tables between Delever entities and provider-side codes. Product mapping links a Delever product UUID to an external product ID per integrat… | admin / integrator |
| ● | Uzbek fiscalisation and goods-marking fields threaded through the catalogue | IKPU (ИКПУ — the state product/service classification code) and package code (Код упаковки) appear as required fields at every priceable level of the catalogue: on products, on variants, on group modifiers and… | admin / finance / compliance |
| ◐ | Website SEO settings (Поисковая оптимизация) | Per-tenant SEO configuration for the generated website. Sets the home page H1 heading and body text, plus meta title (recommended under 60 chars) and meta description (under 160 chars). Category and product pa… | marketing / admin |
| ◐ | Website static pages (Статические страницы) | A mini-CMS for standalone informational pages independent of the catalogue — public offer, delivery calculator, restaurant rules, about the company, blog/news, customer reviews, FAQ. Each page has a name (used… | marketing / content manager |
| ◐ | Website home page merchandising (Главная страница) | Controls what products appear on the site's landing page before the customer opens the menu. Two modes: manual — build named product groups per language ('Recommended', 'Popular', 'New'), hand-picking members,… | marketing |
| ◐ | Mobile app channel settings (Мобильное приложение) | Configures the customer-facing iOS and Android apps independently of each other. Each platform gets: payment types available at checkout (e.g. Cash, Payme, Click), order/fulfilment types (Delivery, Pickup), an… | admin |
| ◐ | QR dine-in menu channel (Настройки QR-зал) | Configures the QR-code menu on restaurant tables, with three distinct operating modes selected by 'QR menu type': (1) view-only QR menu — the guest browses prices and photos, a waiter takes the order; (2) QR f… | admin / guest |
| ◐ | Admin-panel order creation channel (Настройки создания заказов в админ-панели) | Treats manual order entry by call-centre operators and cashiers as its own configurable sales channel: which payment types an operator may choose, which order types are available, whether the operator can flag… | call-centre operator / cash… |
| ◐ | POS integration: Jowi | Per-branch POS binding with three configuration tabs — Credentials (Полномочия), Branches (Филиалы) and Products (Товары) — following the same shape as the other POS adapters: authenticate once, bind Delever b… | admin |
| ◐ | POS integration: Poster | Per-branch POS binding with the widest tab set after iiko — Credentials, Branches, Couriers (Курьеры), Products (Продукты), Discounts (Скидка) and Payment types (Типы оплаты). The presence of a Couriers tab me… | admin |
| ◐ | POS integrations without documented detail: Paloma 365, Neon Alisa, Yaros, Cl… | Six further POS systems appear as named, first-class integrations in the POS-системы menu with dedicated pages, but the pages are stubs containing only the provider name. They confirm the breadth of POS covera… | admin |
| ◐ | Order aggregator: Wolt | Inbound aggregator integration with Wolt's marketplace, configured with a Branches tab (bind Delever branches to Wolt venues) and a Payment types tab (map Wolt payment methods to Delever payment types). Docume… | admin / kitchen |
| ◐ | Order aggregator: Click Mini-App | Inbound order channel through the mini-app storefront inside the Click super-app — Click being one of Uzbekistan's two dominant payment/super-app platforms. Listed as a first-class aggregator with its own page… | admin |
| ◐ | Order aggregator: My Uzbekistan | Fully documented aggregator onboarding with a five-step flow that reveals the generic aggregator model. (1) Create a sales channel in Settings → Orders → Sales channels with a name, type and active status. (2)… | admin |
| ◐ | Delivery service: Noor | Outbound courier sourcing from Noor. Configuration: an authorisation token from Noor; a courier equipment type applied to every matching order, chosen from Standard bag (Стандарт сумка), Thermal bag (Термосумк… | admin / dispatcher |
| ◐ | Delivery service: Wolt Drive | Outbound courier sourcing from Wolt's standalone logistics product. Credentials are a Token, Merchant ID and Secret Client. Branches are added one at a time, each requiring a Venue ID supplied by Wolt for that… | admin / dispatcher |
| ◐ | Delivery service: Millenium | Outbound courier sourcing from Millenium, a local courier service. Credentials are Token, Merchant ID and Secret Client. A toggle — 'count the Millenium delivery amount as the order's delivery amount' — select… | admin / dispatcher |
| ◐ | Payment gateway: Atmos | Acquiring integration with Atmos, an Uzbek payment processor. It has a dedicated page in both documentation versions (page body is a stub), and it appears by name in two other places that confirm it is live: t… | admin / finance |
| ◐ | Payment gateway: Kaspi | Acquiring integration with Kaspi, Kazakhstan's dominant bank and super-app. Configured through a Branches tab binding Delever branches to Kaspi merchant records; documented as a screencast. Kaspi also appears… | admin / finance |
| ◐ | Analytics services: Google Tag Manager, Google Analytics 4 and Google Search… | The analytics category contains exactly one integration surface, wiring the tenant's generated website to three Google services. Search Console: paste the google-site-verification HTML meta tag into the 'Googl… | marketing / admin |
| ◐ | SMS provider: Playmobile (Playmobile.uz) | The alternative SMS gateway, configured identically to Eskiz: provider chosen from the dropdown (Playmobile), the username or email registered in the Playmobile cabinet, the account password or API key, and th… | admin |
| ○ | Multi-brand site switcher (Дополнительные сайты / бренды) | Lets a group operating several brands surface sibling brand sites as clickable logos in the site header on both desktop and mobile, so a customer can jump between the group's pizzeria, sushi bar and cafe witho… | admin / marketing |
| ○ | Order aggregator: Chocofood | Inbound aggregator integration listed with a dedicated page in both documentation versions; page content is a stub with only the provider name. | admin / kitchen |
| ○ | Order aggregator: Foody | Inbound aggregator integration with a dedicated page in both documentation versions; page content is a stub with only the provider name. | admin / kitchen |
| ○ | Order aggregator: Express24 | Inbound aggregator integration present in the earlier documentation version with Branches and Payment types configuration tabs, documented as screencasts. Absent from the current documentation version's aggreg… | admin / kitchen |
| ○ | Delivery service: D-Courier (D-courier) | A delivery-service integration listed in the earlier documentation version's Службы доставки menu; the page is a stub with only the name. The naming ('D-' prefix, as with 'D-aggregator Atmos' in the content-se… | admin / dispatcher |
| ○ | Payment gateway: Epay | Acquiring integration with Epay (Halyk Bank's Kazakhstan payment gateway), with a dedicated page documented as a screencast only. | admin / finance |

**Open questions this section raises**

- Is D-Courier (D-courier) a Delever first-party courier fleet rather than a third-party integration? The 'D-' prefix matches 'D-aggregator Atmos', suggesting Delever-operated services. If Delever sells owned-fleet logistics, that is a separate business line Qoida should decide whether to match — the…
- Express24 appears with Branches and Payment-types configuration in the older documentation tree but is absent from the current one, while Click Mini-App, Uzum Delivery, yTimes and Google Tag Manager are new. Is Express24 deprecated, commercially blocked, or merely undocumented? Qoida should confirm…
- What is 'D-aggregator Atmos' in the per-channel payment matrix, and does it mean Delever acts as a payment facilitator aggregating Atmos on tenants' behalf rather than each tenant holding its own Atmos contract? If Qoida were to do the same it would carry payment-licensing obligations; if not, the…
- Delever's stop-list is expressed as a numeric available quantity per product per branch menu (1C product-stock endpoint), and the dynamic aggregator integration has a 'send products to the stop list at remaining quantity N' threshold. Qoida ADR 0017 is currently BINARY-only availability. Does Qoida…
- My Uzbekistan documents independent per-channel product pricing, and the 1C price endpoint has an is_update_aggregators_price flag deciding whether a price change propagates to aggregators. Does Qoida's ADR 0018 pricing model support price as a function of (product, sales channel), and what is the…
- Delever's generic aggregator 'Client secret' is base64(login:password) of a real panel user, and the website onboarding asks tenants to hand over Cloudflare and domain-registrar credentials to a Delever manager. Both are security defects. Qoida presumably will not copy them — but what is the replac…
- Fiscal identity (INN) can be set per branch, overriding the company INN, and Google Pay merchant profiles can be per branch 'if you want to split payments across legal entities'. Does this mean a single Qoida tenant may contain branches belonging to different legal entities, and if so how does that…
- The QR-hall channel supports 'pull up and pay an existing bill' — reading and settling a ticket a waiter opened in the POS. That is a substantially harder POS capability than pushing an order. Should ADR 0011's capability taxonomy include read-open-ticket and settle-ticket as declared capabilities,…

### Личный кабинет (Account) and BETA версия - V2

This section covers two things that are only superficially related. "Личный кабинет" is the signed-in staff member's own profile page (route /home/account) — a three-tab screen with personal sales statistics, editable personal data, and UI personalization. "BETA версия - V2" is Delever's mechanism for rolling out a rewritten admin panel: a set of tenant-global feature toggles that swap individual modules (orders, dashboard, kitchen, balance, bot settings) between the old and new implementation, plus a restructured 8-item sidebar. The section's richest page is "Admin Dashboard Architecture", an English-language route map of the entire V2 SPA that states the navigation rules: every sidebar section is gated by a `get` permission, and further restricted by "module locks" and "business type". Documentation quality here is uneven — the account page itself is almost entirely video and screensh…

| | Delever capability | What it does | Who |
|---|---|---|---|
| ● | Beta-version control moved into the account page (Раздел «Бета-версия» в личн… | The switch that enables or disables beta features was relocated out of the settings area into the personal account page: the user opens Личный кабинет, opens the «Бета-версия» section, and turns on the beta fe… | admin, manager |
| ● | Updated sidebar navigation (Обновлённое боковое меню) | The V2 beta replaces the old navigation with an eight-item top-level sidebar: Dashboard (Дашборд), Orders (Заказы), Clients (Клиенты), Catalog (Каталог), Marketing (Маркетинг), Content (Контент), Couriers (Кур… | all admin-panel roles |
| ● | Admin Dashboard Architecture route map (Admin Dashboard Architecture) | An English-language specification of the V2 admin SPA's complete route tree, published under BETA V2. It enumerates nine sidebar sections plus a tenth group of 'contextual routes' that are registered but not s… | admin, integrator |
| ● | Permission-gated navigation (доступ по разрешению `get`) | A sidebar section renders only when the signed-in user holds the associated `get` permission for it. Route existence and route visibility are separate concerns — a route can be registered and still be unreacha… | admin, all roles |
| ● | Module locks (Module locks) | A second, independent gate on top of permissions: even when a route exists and the user has the permission, a 'module lock' can withhold access. This is the entitlement layer — the tenant's plan or purchased m… | admin, platform operator |
| ● | Per-module version toggles (Управления версиями) | Under Settings → Company → 'Управления версиями' there is a panel of on/off switches that flip individual modules between the legacy and the v2 implementation, saved together with a Save button. The documented… | admin, manager |
| ◐ | Personal account / profile page (Личный кабинет) | A self-service page for the currently signed-in admin-panel user, reached from the bottom of the sidebar (person icon) and routed at /home/account. It is organised as three tabs — Statistics, Personal Data, Pe… | any admin-panel user (opera… |
| ◐ | Personal sales statistics by sales channel (Статистика — заказы по каналам) | A table on the account page counting the orders attributable to this individual user over the selected period, broken down by the channel the order came through: Admin panel (Админ), Telegram bot (Бот), Websit… | operator, manager |
| ◐ | Personal revenue statistics by payment method (Статистика — суммы по типам оп… | A second table on the same tab totalling the money this user's orders brought in over the period, split by payment instrument: bank transfer (Перечисление), cash (Наличные), Click, Uzum, Payze, Payme, Uzcard,… | operator, manager |
| ◐ | Personal data tab (Личные данные) | Lets the signed-in user view and edit their own account details from inside the panel rather than asking an administrator. Demonstrated only in an embedded video, so the exact editable field set is not stated… | any admin-panel user |
| ◐ | Business-type-driven navigation (business type) | The tenant carries a business type that changes both which routes are available and where the user lands. Specifically, tenants of the courier-service business type default to /home/deliveries instead of the n… | admin, platform operator |
| ◐ | Embedded BI workspace (DataLens/Metabase дашборд) | The primary Dashboard route (/home/dashboard/datalens) does not render a hand-built dashboard — it embeds a DataLens or Metabase workspace inside the admin panel. Analytics is delegated to an external BI tool… | manager, admin |
| ◐ | Reports suite (Отчёты) | Four report groups hang off the V2 Dashboard section. Order reports (/home/dashboard/order-reports): All Reports (combined), Aggregator Report, Daily Report, Product Report, Order Report, External Delivery Rep… | manager, admin |
| ◐ | Telephony and softphone (Телефония / Звонки) | The panel embeds call-centre telephony: a softphone is available from the sidebar, calls can be initiated at /home/calls/new, the call list lives at /home/telephony/calls, and call activity is reported under C… | operator, call-centre manag… |
| ◐ | Deliveries module (Deliveries) | A delivery-record management screen at /home/deliveries, registered as a contextual route rather than a sidebar item, and the default landing route for courier-service tenants. It treats a delivery as the prim… | dispatcher, courier manager |
| ◐ | Kitchen display system routes (KDS) | Dedicated kitchen-display routes under /home/kds, listed separately from the Kitchen section's VDU screen. Kitchen itself covers kitchen orders, a buffer of held orders, VDU, product sales, distribution, order… | kitchen, expeditor |
| ◐ | Loyalty programme placed under Marketing (Программа лояльности) | In V2 the loyalty programme is a Marketing screen at /home/marketing/loyalty, sitting alongside discounts, referral URLs, triggers, notifications and review management. In V1 it lived under order settings. | marketing manager |
| ◐ | Content section split out (Контент) | V2 gives publishable customer-facing content its own top-level section with eleven screens: promotional stocks, popups, banners, news, careers/vacancies, recipes, gallery/events, stories, tenders, site reviews… | marketing manager, content… |
| ◐ | Change history / audit (История изменений) | An audit screen at /home/settings/change-history for reviewing changes made in the panel. | admin |
| ◐ | Geozones (Геозоны) | A settings group at /home/settings/geozones covering delivery zones, per-branch geozones and regions — the spatial rules determining which branch serves which address and whether an address is deliverable at a… | admin, operations manager |
| ○ | Personalization tab (Персонализация) | Per-user interface preferences, presented as its own tab and demonstrated in a second embedded video. Positioned as personal (unlike the beta toggles, which the docs explicitly say are global), so this is wher… | any admin-panel user |
| ○ | Forecasting (Прогнозирование) | A product-level forecasting screen at /home/dashboard/forecasting that shows predicted demand per product. | manager, kitchen |
| ○ | Reservations (Бронирования) | Table-reservation management at /home/reservations, reached from the order workflow rather than the sidebar. Elsewhere the docs describe it as fast management of table reserves and the customers holding them. | operator, host |
| ○ | Repeat order (Repeat Order) | A dedicated route /home/orders/repeat/:id that recreates an existing order as a new one, distinct from creating (/home/orders/create) and editing (/home/orders/:id). | operator |
| ○ | Aggregator partners setting, conditionally shown (Партнёры / Агрегатор) | A Settings screen at /home/settings/aggregator for configuring aggregator partners, shown only 'when enabled' — an example of a settings screen behind a module lock rather than a permission. | admin, platform operator |

**Open questions this section raises**

- The account page's two substantive tabs — Личные данные and Персонализация — are documented only as embedded screen-capture videos with zero prose, so the actual editable fields are unknown. Does 'Personal Data' include self-service password change, phone/login change, or MFA? If yes, Qoida must de…
- What is actually in 'Персонализация'? Language, theme, default branch, table column layout, notification sound? Whatever it is, it is the only per-user-scoped state in this section and Qoida has no staff-preferences store.
- The beta toggle has moved at least twice — the V1 docs say it was relocated INTO Личный кабинет → «Бета-версия», while the newer V2 docs put the same toggles under Settings → Company → Управления версиями. Which is current is not stated. Qoida should skip the intermediate mistake and put tenant-sco…
- Beta activation is explicitly tenant-global ('applies to all users of your admin panel'), yet the control sits on a personal page. Qoida needs an explicit scope dimension in ADR 0030 config resolution — platform / tenant / branch / user — and the UI should state the scope of every toggle.
- The six version toggles (Заказ v2, Дашборд v2, Баланс v2, Кухонные заказы v2, Настройки бота v2, 'other sections') imply two live implementations of each module coexisting per tenant. Is Qoida willing to carry that cost for ADR 0024 cutover, or will it cut over whole tenants at once? Per-module tog…
- 'Business type' changes both default route and module availability (courier-service tenants land on /home/deliveries). Is Qoida one product or several product shapes on one platform? ADR 0002 does not appear to carry a business-type discriminator, and adding one after launch is expensive.
- 'Module locks' are a gate distinct from permissions. Qoida ADR 0021 (entitlements) is NOT STARTED while ADR 0025 (authorization) exists — the two must be designed to compose, and the frontend must be able to tell 'not permitted' apart from 'not purchased'.
- Analytics is delegated to an embedded DataLens/Metabase workspace. Qoida has no analytics or BI ADR at all. Build, buy, or embed — and if embed, how is per-tenant row-level isolation enforced inside the BI tool, and does the BI tool's hosting satisfy ADR 0034 residency?
