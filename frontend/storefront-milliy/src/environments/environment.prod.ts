// See environment.ts: `ApiClient` reads `APP_CONFIG` from /config.json, not
// this file. `yandexMapsApiKey` is the one value still needed at build time.
export const environment = {
  yandexMapsApiKey: '99847472-f185-464c-b2cb-7b28dd285a8c'
};
